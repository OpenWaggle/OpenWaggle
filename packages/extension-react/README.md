# @openwaggle/extension-react

React UI primitives for OpenWaggle extensions. React and React DOM are peer dependencies; import `@openwaggle/extension-react/styles.css` when you want the default host-aligned styles.
