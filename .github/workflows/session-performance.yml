name: Session Performance

on:
  merge_group:
  workflow_dispatch:
    inputs:
      head_sha:
        description: Exact commit SHA to benchmark
        required: true
        type: string
  workflow_call:
    inputs:
      head_sha:
        description: Exact caller commit SHA to benchmark
        required: true
        type: string

permissions:
  contents: read

jobs:
  session-performance:
    name: Session Performance
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Verify benchmark commit identity
        env:
          DISPATCHED_SHA: ${{ github.sha }}
          EXPECTED_SHA: ${{ inputs.head_sha }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          if [[ "$EVENT_NAME" == "merge_group" && -z "$EXPECTED_SHA" ]]; then
            EXPECTED_SHA="$DISPATCHED_SHA"
          fi
          [[ "$EXPECTED_SHA" =~ ^[0-9a-f]{40}$ ]]
          test "$DISPATCHED_SHA" = "$EXPECTED_SHA"

      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          ref: ${{ inputs.head_sha || github.sha }}

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: 11.15.1

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 24.14.0
          cache: pnpm

      - uses: ./.github/actions/pnpm-install

      - run: pnpm benchmark:session-release
