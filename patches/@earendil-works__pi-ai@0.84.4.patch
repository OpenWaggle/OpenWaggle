diff --git a/dist/api/openai-responses-shared.js b/dist/api/openai-responses-shared.js
index c35771dcc9b2173d0b459d13c6d86e622de45e9a..3f55a72bbae51f21de7e26fe11b2728bfd4c3e2e 100644
--- a/dist/api/openai-responses-shared.js
+++ b/dist/api/openai-responses-shared.js
@@ -137,7 +137,9 @@ export function convertResponsesMessages(model, context, allowedToolCallProvider
                 if (block.type === "thinking") {
                     if (block.thinkingSignature) {
                         const reasoningItem = JSON.parse(block.thinkingSignature);
-                        output.push(reasoningItem);
+                        if (reasoningItem.type !== "compaction" || isSameModel) {
+                            output.push(reasoningItem);
+                        }
                     }
                 }
                 else if (block.type === "text") {
diff --git a/dist/api/openai-responses.d.ts b/dist/api/openai-responses.d.ts
index 41267bd50904069aa0879cc8634ea94bc1045236..7b431baee4581266a093653615b4f6515903ca59 100644
--- a/dist/api/openai-responses.d.ts
+++ b/dist/api/openai-responses.d.ts
@@ -1,5 +1,6 @@
-import type { ResponseCreateParamsStreaming } from "openai/resources/responses/responses.js";
-import type { SimpleStreamOptions, StreamFunction, StreamOptions } from "../types.ts";
+import type { CompactedResponse, ResponseCreateParamsStreaming } from "openai/resources/responses/responses.js";
+import type { Context, Model, SimpleStreamOptions, StreamFunction, StreamOptions } from "../types.ts";
+import type { RetryCallbacks, RetryPolicy } from "../utils/retry.ts";
 export interface OpenAIResponsesOptions extends StreamOptions {
     reasoningEffort?: "minimal" | "low" | "medium" | "high" | "xhigh" | "max";
     reasoningSummary?: "auto" | "detailed" | "concise" | null;
@@ -11,4 +12,9 @@ export interface OpenAIResponsesOptions extends StreamOptions {
  */
 export declare const stream: StreamFunction<"openai-responses", OpenAIResponsesOptions>;
 export declare const streamSimple: StreamFunction<"openai-responses", SimpleStreamOptions>;
+export interface CompactResponsesOptions extends SimpleStreamOptions {
+    retryPolicy?: RetryPolicy;
+    retryCallbacks?: RetryCallbacks;
+}
+export declare function compactResponses(model: Model<"openai-responses" | "azure-openai-responses" | "openai-codex-responses">, context: Context, options?: CompactResponsesOptions): Promise<CompactedResponse>;
 //# sourceMappingURL=openai-responses.d.ts.map
\ No newline at end of file
diff --git a/dist/api/openai-responses.js b/dist/api/openai-responses.js
index 0fb7e8b26f323f2b44a5a4b760bccedc4c3abdd0..ebb125f45a12d0c316c720699082549e5a3db192 100644
--- a/dist/api/openai-responses.js
+++ b/dist/api/openai-responses.js
@@ -13,6 +13,7 @@ import { clampOpenAIPromptCacheKey } from "./openai-prompt-cache.js";
 import { convertResponsesMessages, convertResponsesTools, processResponsesStream } from "./openai-responses-shared.js";
 import { buildBaseOptions } from "./simple-options.js";
 const OPENAI_TOOL_CALL_PROVIDERS = new Set(["openai", "openai-codex", "opencode"]);
+const CODEX_JWT_CLAIM_PATH = "https://api.openai.com/auth";
 // OpenAI Responses rejects max_output_tokens below 16: https://github.com/earendil-works/pi/issues/6265
 const OPENAI_RESPONSES_MIN_OUTPUT_TOKENS = 16;
 function hasHeader(headers, name) {
@@ -32,6 +33,24 @@ function getClientApiKey(provider, apiKey, headers) {
         return "unused";
     throw new Error(`No API key for provider: ${provider}`);
 }
+function getCodexAccountId(token) {
+    try {
+        const parts = token.split(".");
+        if (parts.length !== 3)
+            throw new Error("Invalid token");
+        const encodedPayload = parts[1].replace(/-/g, "+").replace(/_/g, "/");
+        const paddedPayload = encodedPayload.padEnd(Math.ceil(encodedPayload.length / 4) * 4, "=");
+        const bytes = Uint8Array.from(atob(paddedPayload), (character) => character.charCodeAt(0));
+        const payload = JSON.parse(new TextDecoder().decode(bytes));
+        const accountId = payload?.[CODEX_JWT_CLAIM_PATH]?.chatgpt_account_id;
+        if (!accountId)
+            throw new Error("No account ID in token");
+        return accountId;
+    }
+    catch {
+        throw new Error("Failed to extract accountId from token");
+    }
+}
 function detectSessionAffinityFormat(model) {
     return model.provider === "openrouter" || model.baseUrl.includes("openrouter.ai") ? "openrouter" : "openai";
 }
@@ -50,6 +69,7 @@ function resolveCacheRetention(cacheRetention, env) {
 }
 function getCompat(model) {
     return {
+        compactionBaseUrl: model.compat?.compactionBaseUrl,
         supportsDeveloperRole: model.compat?.supportsDeveloperRole ?? true,
         sessionAffinityFormat: model.compat?.sessionAffinityFormat ?? detectSessionAffinityFormat(model),
         supportsLongCacheRetention: model.compat?.supportsLongCacheRetention ?? true,
@@ -160,7 +180,49 @@ export const streamSimple = (model, context, options) => {
         reasoningEffort,
     });
 };
-function createClient(model, context, apiKey, optionsHeaders, fetch, sessionId) {
+export async function compactResponses(model, context, options) {
+    if (model.compat?.supportsCompaction !== true) {
+        throw new Error(`Responses compaction is not supported by ${model.provider}/${model.id}`);
+    }
+    const apiKey = getClientApiKey(model.provider, options?.apiKey, options?.headers);
+    const cacheRetention = resolveCacheRetention(options?.cacheRetention, options?.env);
+    const cacheSessionId = cacheRetention === "none" ? undefined : options?.sessionId;
+    const compat = getCompat(model);
+    const client = createClient(model, context, apiKey, options?.headers, options?.fetch, cacheSessionId, compat.compactionBaseUrl);
+    const input = convertResponsesMessages(model, context, OPENAI_TOOL_CALL_PROVIDERS, {
+        includeSystemPrompt: false,
+        toolOptions: {
+            supportsStrictMode: compat.supportsStrictMode,
+            supportsOpenAIGrammarTools: compat.supportsOpenAIGrammarTools,
+        },
+    });
+    let params = {
+        model: model.id,
+        input,
+        instructions: context.systemPrompt || undefined,
+        prompt_cache_key: cacheRetention === "none" ? undefined : clampOpenAIPromptCacheKey(options?.sessionId),
+        prompt_cache_retention: getPromptCacheRetention(compat, cacheRetention),
+    };
+    const nextParams = await options?.onPayload?.(params, model);
+    if (nextParams !== undefined) {
+        params = nextParams;
+    }
+    const requestOptions = {
+        ...(options?.signal ? { signal: options.signal } : {}),
+        ...(options?.timeoutMs !== undefined ? { timeout: options.timeoutMs } : {}),
+        maxRetries: 0,
+    };
+    const { data, response } = await retryProviderRequest(() => client.responses.compact(params, requestOptions).withResponse(), {
+        maxRetries: options?.maxRetries,
+        maxRetryDelayMs: options?.maxRetryDelayMs,
+        signal: options?.signal,
+        retryPolicy: options?.retryPolicy,
+        retryCallbacks: options?.retryCallbacks,
+    });
+    await options?.onResponse?.({ status: response.status, headers: headersToRecord(response.headers) }, model);
+    return data;
+}
+function createClient(model, context, apiKey, optionsHeaders, fetch, sessionId, baseUrlOverride) {
     const compat = getCompat(model);
     const headers = { "User-Agent": getPiUserAgent(), ...model.headers };
     if (model.provider === "github-copilot") {
@@ -186,9 +248,21 @@ function createClient(model, context, apiKey, optionsHeaders, fetch, sessionId)
     if (optionsHeaders) {
         Object.assign(headers, optionsHeaders);
     }
+    if (model.api === "openai-codex-responses") {
+        if (!hasHeader(headers, "Authorization"))
+            headers.Authorization = `Bearer ${apiKey}`;
+        if (!hasHeader(headers, "chatgpt-account-id") && apiKey !== "unused")
+            headers["chatgpt-account-id"] = getCodexAccountId(apiKey);
+        if (!hasHeader(headers, "originator"))
+            headers.originator = "pi";
+        if (!hasHeader(headers, "OpenAI-Beta"))
+            headers["OpenAI-Beta"] = "responses=experimental";
+        if (!hasHeader(headers, "User-Agent"))
+            headers["User-Agent"] = getPiUserAgent();
+    }
     return new OpenAI({
         apiKey,
-        baseURL: model.baseUrl,
+        baseURL: baseUrlOverride ?? model.baseUrl,
         dangerouslyAllowBrowser: true,
         fetch,
         defaultHeaders: headers,
diff --git a/dist/providers/openai-codex.models.js b/dist/providers/openai-codex.models.js
index c53dcaa13ee7106a07383697302e63962bb23f9d..d2be610a021040efdc35722564b8dfe5df7c82db 100644
--- a/dist/providers/openai-codex.models.js
+++ b/dist/providers/openai-codex.models.js
@@ -2,5 +2,27 @@
 // Do not edit manually - run 'npm run generate-models' to update
 import values from "./data/openai-codex.json" with { type: "json" };
 import { flattenModelCatalog } from "../model-catalog.js";
-export const OPENAI_CODEX_MODELS = flattenModelCatalog("openai-codex", values);
+const COMPACTION_CAPABLE_MODEL_IDS = new Set([
+    "gpt-5.3-codex-spark",
+    "gpt-5.4",
+    "gpt-5.4-mini",
+    "gpt-5.5",
+    "gpt-5.6-luna",
+    "gpt-5.6-sol",
+    "gpt-5.6-terra",
+]);
+const openAICodexModels = flattenModelCatalog("openai-codex", values);
+export const OPENAI_CODEX_MODELS = Object.fromEntries(Object.entries(openAICodexModels).map(([modelId, model]) => COMPACTION_CAPABLE_MODEL_IDS.has(modelId)
+    ? [
+        modelId,
+        {
+            ...model,
+            compat: {
+                ...model.compat,
+                supportsCompaction: true,
+                compactionBaseUrl: "https://chatgpt.com/backend-api/codex",
+            },
+        },
+    ]
+    : [modelId, model]));
 //# sourceMappingURL=openai-codex.models.js.map
\ No newline at end of file
diff --git a/dist/providers/openai.models.js b/dist/providers/openai.models.js
index b64f4cd6014db2429b22dec1923d80460b4cee49..a32c2d65828101b145d8961eff7ae50012b9ef31 100644
--- a/dist/providers/openai.models.js
+++ b/dist/providers/openai.models.js
@@ -2,5 +2,9 @@
 // Do not edit manually - run 'npm run generate-models' to update
 import values from "./data/openai.json" with { type: "json" };
 import { flattenModelCatalog } from "../model-catalog.js";
-export const OPENAI_MODELS = flattenModelCatalog("openai", values);
+const COMPACTION_CAPABLE_MODEL_IDS = new Set(["gpt-5.2", "gpt-5.6-sol"]);
+const openAIModels = flattenModelCatalog("openai", values);
+export const OPENAI_MODELS = Object.fromEntries(Object.entries(openAIModels).map(([modelId, model]) => COMPACTION_CAPABLE_MODEL_IDS.has(modelId)
+    ? [modelId, { ...model, compat: { ...model.compat, supportsCompaction: true } }]
+    : [modelId, model]));
 //# sourceMappingURL=openai.models.js.map
\ No newline at end of file
diff --git a/dist/types.d.ts b/dist/types.d.ts
index 2bb23658ee692343e4561ac461bd0e8d9328c5a1..20b4414661ef01060c7a2f013b4eef40dcb3abad 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -515,6 +515,10 @@ export interface OpenAICompletionsCompat {
 }
 /** Compatibility settings for OpenAI Responses APIs. */
 export interface OpenAIResponsesCompat {
+    /** Whether this transport implements the public Responses Compaction protocol. Default: false. */
+    supportsCompaction?: boolean;
+    /** Optional public Responses Compaction base URL when it differs from the normal Responses transport. */
+    compactionBaseUrl?: string;
     /** Whether the provider supports the `developer` role (vs `system`). Default: true. */
     supportsDeveloperRole?: boolean;
     /** Session-affinity header format: `openai` sends `session_id` and `x-client-request-id`; `openai-nosession` sends `x-client-request-id`; `openrouter` sends `x-session-id`. Does not affect the `prompt_cache_key` body param, which is governed by cache retention. Default: auto-detected. */
diff --git a/dist/utils/provider-retry.d.ts b/dist/utils/provider-retry.d.ts
index 1872cd204eba7d413f452ccc747fa28a920b3ac7..3cffac5ece4fe1f5d8df2149311e0ae3de4112eb 100644
--- a/dist/utils/provider-retry.d.ts
+++ b/dist/utils/provider-retry.d.ts
@@ -1,7 +1,10 @@
+import type { RetryCallbacks, RetryPolicy } from "./retry.ts";
 interface ProviderRetryOptions {
     maxRetries?: number;
     maxRetryDelayMs?: number;
     signal?: AbortSignal;
+    retryPolicy?: RetryPolicy;
+    retryCallbacks?: RetryCallbacks;
 }
 /**
  * Reproduce the retry behavior used by the OpenAI and Anthropic SDKs while making
diff --git a/dist/utils/provider-retry.js b/dist/utils/provider-retry.js
index a08fefd7e254f494f838ffa95affd6bf279973db..580bd3fd5a65b6eb2a388512fef113c250e267b8 100644
--- a/dist/utils/provider-retry.js
+++ b/dist/utils/provider-retry.js
@@ -1,3 +1,4 @@
+import { isTerminalProviderErrorMessage } from "./retry.js";
 const DEFAULT_MAX_RETRY_DELAY_MS = 60_000;
 function isProviderError(error) {
     if (!(error instanceof Error) || !("status" in error) || !("headers" in error))
@@ -73,21 +74,47 @@ function abortableSleep(ms, signal) {
  * disable the limit.
  */
 export async function retryProviderRequest(request, options = {}) {
-    const maxRetries = options.maxRetries ?? 0;
+    const policy = options.retryPolicy;
+    const maxRetries = policy ? (policy.enabled ? policy.maxRetries : 0) : (options.maxRetries ?? 0);
     let retriesRemaining = maxRetries;
+    let retryAttempt = 0;
     for (;;) {
         try {
             // Each retry is a fresh SDK request, so X-Stainless-Retry-Count remains zero.
-            return await request();
+            const result = await request();
+            if (retryAttempt > 0)
+                await options.retryCallbacks?.onRetryFinished?.(true, retryAttempt);
+            return result;
         }
         catch (error) {
-            if (options.signal?.aborted)
+            const errorMessage = error instanceof Error ? error.message : String(error);
+            if (options.signal?.aborted) {
+                if (retryAttempt > 0)
+                    await options.retryCallbacks?.onRetryFinished?.(false, retryAttempt, errorMessage);
                 throw createAbortError();
-            if (retriesRemaining <= 0 || !isProviderError(error) || !isRetryableProviderError(error))
+            }
+            const retryableByProvider = isProviderError(error) && isRetryableProviderError(error);
+            const retryableByPolicy = !policy || !isTerminalProviderErrorMessage(errorMessage);
+            if (retriesRemaining <= 0 || !retryableByProvider || !retryableByPolicy) {
+                if (retryAttempt > 0)
+                    await options.retryCallbacks?.onRetryFinished?.(false, retryAttempt, errorMessage);
                 throw error;
+            }
             const retryIndex = maxRetries - retriesRemaining;
             retriesRemaining--;
-            await abortableSleep(getRetryDelayMs(error, retryIndex, options.maxRetryDelayMs), options.signal);
+            retryAttempt = retryIndex + 1;
+            const delayMs = policy
+                ? policy.baseDelayMs * 2 ** retryIndex
+                : getRetryDelayMs(error, retryIndex, options.maxRetryDelayMs);
+            await options.retryCallbacks?.onRetryScheduled?.(retryAttempt, maxRetries, delayMs, errorMessage);
+            try {
+                await abortableSleep(delayMs, options.signal);
+            }
+            catch (sleepError) {
+                await options.retryCallbacks?.onRetryFinished?.(false, retryAttempt, errorMessage);
+                throw sleepError;
+            }
+            await options.retryCallbacks?.onRetryAttemptStart?.();
         }
     }
 }
diff --git a/dist/utils/retry.d.ts b/dist/utils/retry.d.ts
index 19a199b4bfbe6956d9ec47a3943bbb4845759d36..2a93f55ff11b1912597aa3678966b26d467f79a1 100644
--- a/dist/utils/retry.d.ts
+++ b/dist/utils/retry.d.ts
@@ -50,4 +50,8 @@ export declare function retryAssistantCall(produce: () => Promise<AssistantMessa
  * before restarting the assistant turn.
  */
 export declare function isRetryableAssistantError(message: AssistantMessage): boolean;
+/** Return whether an error string is a known terminal quota, budget, or billing failure. */
+export declare function isTerminalProviderErrorMessage(errorMessage: string): boolean;
+/** Classify a provider/transport error string using the shared Pi retry rules. */
+export declare function isRetryableProviderErrorMessage(errorMessage: string): boolean;
 //# sourceMappingURL=retry.d.ts.map
\ No newline at end of file
diff --git a/dist/utils/retry.js b/dist/utils/retry.js
index 65c7c9efd7667445b711fcd43549062ee3eb5ecf..ab631ba7a861eedcabfc1d2e749023489235696b 100644
--- a/dist/utils/retry.js
+++ b/dist/utils/retry.js
@@ -166,8 +166,13 @@ export async function retryAssistantCall(produce, policy, signal, callbacks) {
 export function isRetryableAssistantError(message) {
     if (message.stopReason !== "error" || !message.errorMessage)
         return false;
-    const errorMessage = message.errorMessage;
-    if (NON_RETRYABLE_PROVIDER_LIMIT_ERROR_PATTERN.test(errorMessage))
+    return isRetryableProviderErrorMessage(message.errorMessage);
+}
+export function isTerminalProviderErrorMessage(errorMessage) {
+    return NON_RETRYABLE_PROVIDER_LIMIT_ERROR_PATTERN.test(errorMessage);
+}
+export function isRetryableProviderErrorMessage(errorMessage) {
+    if (isTerminalProviderErrorMessage(errorMessage))
         return false;
     return RETRYABLE_PROVIDER_ERROR_PATTERN.test(errorMessage);
 }
