--- a/dist/esm/adapters/text.js
+++ b/dist/esm/adapters/text.js
@@ -129,6 +129,42 @@
     let hasEmittedRunStarted = false;
     let hasEmittedTextMessageStart = false;
     let hasEmittedStepStarted = false;
+    let hasEmittedRunFinished = false;
+    const codexSseTraceEnabled = typeof process !== "undefined" && process.env.OPENWAGGLE_ENABLE_CODEX_SSE_TRACE === "1";
+    let hasObservedToolCalls = false;
+    let terminalDecision = "none";
+    const traceChunkType = (chunkType) => {
+      if (!codexSseTraceEnabled) {
+        return;
+      }
+      console.log("[OpenAI Adapter SSE Trace]", { chunkType });
+    };
+    const traceTerminalResponse = (chunkType, response, decision) => {
+      if (!codexSseTraceEnabled) {
+        return;
+      }
+      console.log("[OpenAI Adapter SSE Trace]", {
+        chunkType,
+        responseStatus: response.status,
+        incompleteReason: response.incomplete_details?.reason ?? null,
+        outputItems: response.output.map((item) => {
+          const status = "status" in item && typeof item.status === "string" ? item.status : null;
+          return status ? `${item.type}:${status}` : item.type;
+        }),
+        toolCallMetadataSize: toolCallMetadata.size,
+        terminalDecision: decision
+      });
+    };
+    const traceErrorChunk = (decision) => {
+      if (!codexSseTraceEnabled) {
+        return;
+      }
+      console.log("[OpenAI Adapter SSE Trace]", {
+        chunkType: "error",
+        toolCallMetadataSize: toolCallMetadata.size,
+        terminalDecision: decision
+      });
+    };
     try {
       for await (const chunk of stream) {
         chunkCount++;
@@ -174,7 +210,44 @@
             }
           };
         };
-        if (chunk.type === "response.created" || chunk.type === "response.incomplete" || chunk.type === "response.failed") {
+        const hasToolCallSignal = hasObservedToolCalls || toolCallMetadata.size > 0;
+        if (chunk.type === "response.incomplete" || chunk.type === "response.completed" || chunk.type === "response.failed") {
+          const completedHasFunctionCalls = chunk.type === "response.completed" && (hasToolCallSignal || chunk.response.output.some((item) => item.type === "function_call"));
+          const decision = terminalDecision !== "none" ? "ignored_after_terminal" : chunk.type === "response.completed" ? completedHasFunctionCalls ? "run_finished_tool_calls" : "run_finished_stop" : chunk.type === "response.incomplete" ? hasToolCallSignal ? "run_finished_tool_calls" : "run_error" : "run_error";
+          traceTerminalResponse(chunk.type, chunk.response, decision);
+          if (terminalDecision !== "none") {
+            continue;
+          }
+        } else if (chunk.type !== "response.output_text.delta" && chunk.type !== "response.reasoning_text.delta" && chunk.type !== "response.reasoning_summary_text.delta") {
+          traceChunkType(chunk.type);
+        }
+        if (chunk.type === "response.incomplete" && hasToolCallSignal) {
+          model = chunk.response.model;
+          if (hasEmittedTextMessageStart) {
+            yield {
+              type: "TEXT_MESSAGE_END",
+              messageId,
+              model: chunk.response.model || model || options.model,
+              timestamp
+            };
+          }
+          yield {
+            type: "RUN_FINISHED",
+            runId,
+            model: chunk.response.model || model || options.model,
+            timestamp,
+            usage: {
+              promptTokens: chunk.response.usage?.input_tokens || 0,
+              completionTokens: chunk.response.usage?.output_tokens || 0,
+              totalTokens: chunk.response.usage?.total_tokens || 0
+            },
+            finishReason: "tool_calls"
+          };
+          terminalDecision = "tool_calls";
+          hasEmittedRunFinished = true;
+          continue;
+        }
+        if (chunk.type === "response.created" || chunk.type === "response.failed" || chunk.type === "response.incomplete") {
           model = chunk.response.model;
           hasStreamedContentDeltas = false;
           hasStreamedReasoningDeltas = false;
@@ -182,7 +255,11 @@
           hasEmittedStepStarted = false;
           accumulatedContent = "";
           accumulatedReasoning = "";
+          if (chunk.type === "response.incomplete" && hasToolCallSignal) {
+            continue;
+          }
           if (chunk.response.error) {
+            terminalDecision = "error";
             yield {
               type: "RUN_ERROR",
               runId,
@@ -190,8 +267,10 @@
               timestamp,
               error: chunk.response.error
             };
+            continue;
           }
           if (chunk.response.incomplete_details) {
+            terminalDecision = "error";
             yield {
               type: "RUN_ERROR",
               runId,
@@ -201,6 +280,7 @@
                 message: chunk.response.incomplete_details.reason ?? ""
               }
             };
+            continue;
           }
         }
         if (chunk.type === "response.output_text.delta" && chunk.delta) {
@@ -318,6 +398,7 @@
         if (chunk.type === "response.output_item.added") {
           const item = chunk.item;
           if (item.type === "function_call" && item.id) {
+            hasObservedToolCalls = true;
             if (!toolCallMetadata.has(item.id)) {
               toolCallMetadata.set(item.id, {
                 index: chunk.output_index,
@@ -368,6 +449,9 @@
           };
         }
         if (chunk.type === "response.completed") {
+          const hasFunctionCalls = hasObservedToolCalls || toolCallMetadata.size > 0 || chunk.response.output.some(
+            (item) => item.type === "function_call"
+          );
           if (hasEmittedTextMessageStart) {
             yield {
               type: "TEXT_MESSAGE_END",
@@ -376,9 +460,6 @@
               timestamp
             };
           }
-          const hasFunctionCalls = chunk.response.output.some(
-            (item) => item.type === "function_call"
-          );
           yield {
             type: "RUN_FINISHED",
             runId,
@@ -391,8 +472,16 @@
             },
             finishReason: hasFunctionCalls ? "tool_calls" : "stop"
           };
+          terminalDecision = hasFunctionCalls ? "tool_calls" : "stop";
+          hasEmittedRunFinished = true;
         }
         if (chunk.type === "error") {
+          if (terminalDecision !== "none") {
+            traceErrorChunk("ignored_after_terminal");
+            continue;
+          }
+          traceErrorChunk("run_error");
+          terminalDecision = "error";
           yield {
             type: "RUN_ERROR",
             runId,
@@ -403,8 +492,29 @@
               code: chunk.code ?? void 0
             }
           };
+          continue;
         }
       }
+      if (terminalDecision === "none" && toolCallMetadata.size > 0) {
+        traceChunkType("stream.end");
+        if (hasEmittedTextMessageStart) {
+          yield {
+            type: "TEXT_MESSAGE_END",
+            messageId,
+            model: model || options.model,
+            timestamp
+          };
+        }
+        yield {
+          type: "RUN_FINISHED",
+          runId,
+          model: model || options.model,
+          timestamp,
+          finishReason: "tool_calls"
+        };
+        terminalDecision = "tool_calls";
+        hasEmittedRunFinished = true;
+      }
     } catch (error) {
       const err = error;
       console.log(
--- a/src/adapters/text.ts
+++ b/src/adapters/text.ts
@@ -263,7 +263,61 @@
     let hasEmittedRunStarted = false
     let hasEmittedTextMessageStart = false
     let hasEmittedStepStarted = false
+    let hasEmittedRunFinished = false
+    const codexSseTraceEnabled =
+      typeof process !== 'undefined' &&
+      process.env.OPENWAGGLE_ENABLE_CODEX_SSE_TRACE === '1'
+    let hasObservedToolCalls = false
+    let terminalDecision: 'none' | 'tool_calls' | 'stop' | 'error' = 'none'
 
+    const traceChunkType = (chunkType: string): void => {
+      if (!codexSseTraceEnabled) {
+        return
+      }
+      console.log('[OpenAI Adapter SSE Trace]', { chunkType })
+    }
+
+    const traceTerminalResponse = (
+      chunkType: 'response.incomplete' | 'response.completed' | 'response.failed',
+      response: OpenAI_SDK.Responses.Response,
+      decision:
+        | 'run_error'
+        | 'run_finished_tool_calls'
+        | 'run_finished_stop'
+        | 'ignored_after_terminal',
+    ): void => {
+      if (!codexSseTraceEnabled) {
+        return
+      }
+      console.log('[OpenAI Adapter SSE Trace]', {
+        chunkType,
+        responseStatus: response.status,
+        incompleteReason: response.incomplete_details?.reason ?? null,
+        outputItems: response.output.map((item) => {
+          const status =
+            'status' in item && typeof item.status === 'string'
+              ? item.status
+              : null
+          return status ? `${item.type}:${status}` : item.type
+        }),
+        toolCallMetadataSize: toolCallMetadata.size,
+        terminalDecision: decision,
+      })
+    }
+
+    const traceErrorChunk = (
+      decision: 'run_error' | 'ignored_after_terminal',
+    ): void => {
+      if (!codexSseTraceEnabled) {
+        return
+      }
+      console.log('[OpenAI Adapter SSE Trace]', {
+        chunkType: 'error',
+        toolCallMetadataSize: toolCallMetadata.size,
+        terminalDecision: decision,
+      })
+    }
+
     try {
       for await (const chunk of stream) {
         chunkCount++
@@ -318,11 +372,76 @@
             },
           }
         }
-        // handle general response events
+        const hasToolCallSignal =
+          hasObservedToolCalls || toolCallMetadata.size > 0
+
         if (
-          chunk.type === 'response.created' ||
           chunk.type === 'response.incomplete' ||
+          chunk.type === 'response.completed' ||
           chunk.type === 'response.failed'
+        ) {
+          const completedHasFunctionCalls =
+            chunk.type === 'response.completed' &&
+            (hasToolCallSignal ||
+              chunk.response.output.some((item) => item.type === 'function_call'))
+          const decision =
+            terminalDecision !== 'none'
+              ? 'ignored_after_terminal'
+              : chunk.type === 'response.completed'
+                ? completedHasFunctionCalls
+                  ? 'run_finished_tool_calls'
+                  : 'run_finished_stop'
+                : chunk.type === 'response.incomplete'
+                  ? hasToolCallSignal
+                    ? 'run_finished_tool_calls'
+                    : 'run_error'
+                  : 'run_error'
+          traceTerminalResponse(chunk.type, chunk.response, decision)
+          if (terminalDecision !== 'none') {
+            continue
+          }
+        } else if (
+          chunk.type !== 'response.output_text.delta' &&
+          chunk.type !== 'response.reasoning_text.delta' &&
+          chunk.type !== 'response.reasoning_summary_text.delta'
+        ) {
+          traceChunkType(chunk.type)
+        }
+
+        // When the Codex endpoint exhausts its server-side output budget but has
+        // already streamed tool calls, signal the engine to execute them and
+        // continue. The continuation gets a fresh output budget.
+        if (chunk.type === 'response.incomplete' && hasToolCallSignal) {
+          model = chunk.response.model
+          if (hasEmittedTextMessageStart) {
+            yield {
+              type: 'TEXT_MESSAGE_END',
+              messageId,
+              model: chunk.response.model || model || options.model,
+              timestamp,
+            }
+          }
+          yield {
+            type: 'RUN_FINISHED',
+            runId,
+            model: chunk.response.model || model || options.model,
+            timestamp,
+            usage: {
+              promptTokens: chunk.response.usage?.input_tokens || 0,
+              completionTokens: chunk.response.usage?.output_tokens || 0,
+              totalTokens: chunk.response.usage?.total_tokens || 0,
+            },
+            finishReason: 'tool_calls',
+          }
+          terminalDecision = 'tool_calls'
+          hasEmittedRunFinished = true
+          continue
+        }
+        // handle general response events
+        if (
+          chunk.type === 'response.created' ||
+          chunk.type === 'response.failed' ||
+          chunk.type === 'response.incomplete'
         ) {
           model = chunk.response.model
           // Reset streaming flags for new response
@@ -332,7 +451,11 @@
           hasEmittedStepStarted = false
           accumulatedContent = ''
           accumulatedReasoning = ''
+          if (chunk.type === 'response.incomplete' && hasToolCallSignal) {
+            continue
+          }
           if (chunk.response.error) {
+            terminalDecision = 'error'
             yield {
               type: 'RUN_ERROR',
               runId,
@@ -340,8 +463,10 @@
               timestamp,
               error: chunk.response.error,
             }
+            continue
           }
           if (chunk.response.incomplete_details) {
+            terminalDecision = 'error'
             yield {
               type: 'RUN_ERROR',
               runId,
@@ -351,6 +476,7 @@
                 message: chunk.response.incomplete_details.reason ?? '',
               },
             }
+            continue
           }
         }
         // Handle output text deltas (token-by-token streaming)
@@ -519,6 +645,7 @@
         if (chunk.type === 'response.output_item.added') {
           const item = chunk.item
           if (item.type === 'function_call' && item.id) {
+            hasObservedToolCalls = true
             // Store the function name for later use
             if (!toolCallMetadata.has(item.id)) {
               toolCallMetadata.set(item.id, {
@@ -582,6 +709,11 @@
         }
 
         if (chunk.type === 'response.completed') {
+          const hasFunctionCalls =
+            hasObservedToolCalls ||
+            toolCallMetadata.size > 0 ||
+            chunk.response.output.some((item) => item.type === 'function_call')
+
           // Emit TEXT_MESSAGE_END if we had text content
           if (hasEmittedTextMessageStart) {
             yield {
@@ -592,13 +724,6 @@
             }
           }
 
-          // Determine finish reason based on output
-          // If there are function_call items in the output, it's a tool_calls finish
-          const hasFunctionCalls = chunk.response.output.some(
-            (item: unknown) =>
-              (item as { type: string }).type === 'function_call',
-          )
-
           yield {
             type: 'RUN_FINISHED',
             runId,
@@ -611,9 +736,17 @@
             },
             finishReason: hasFunctionCalls ? 'tool_calls' : 'stop',
           }
+          terminalDecision = hasFunctionCalls ? 'tool_calls' : 'stop'
+          hasEmittedRunFinished = true
         }
 
         if (chunk.type === 'error') {
+          if (terminalDecision !== 'none') {
+            traceErrorChunk('ignored_after_terminal')
+            continue
+          }
+          traceErrorChunk('run_error')
+          terminalDecision = 'error'
           yield {
             type: 'RUN_ERROR',
             runId,
@@ -624,8 +757,31 @@
               code: chunk.code ?? undefined,
             },
           }
+          continue
         }
       }
+      // Fallback: if the stream ended without a terminal event but tool calls
+      // were generated, signal continuation so they get executed.
+      if (terminalDecision === 'none' && toolCallMetadata.size > 0) {
+        traceChunkType('stream.end')
+        if (hasEmittedTextMessageStart) {
+          yield {
+            type: 'TEXT_MESSAGE_END',
+            messageId,
+            model: model || options.model,
+            timestamp,
+          }
+        }
+        yield {
+          type: 'RUN_FINISHED',
+          runId,
+          model: model || options.model,
+          timestamp,
+          finishReason: 'tool_calls',
+        }
+        terminalDecision = 'tool_calls'
+        hasEmittedRunFinished = true
+      }
     } catch (error: unknown) {
       const err = error as Error & { code?: string }
       console.log(
