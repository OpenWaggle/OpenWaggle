diff --git a/dist/core/agent-session.d.ts b/dist/core/agent-session.d.ts
index 5620ac326bd3885819f4dbca401522c3058a640b..0111dd45a8771ae83c25d48b1bf1f3347643b490 100644
--- a/dist/core/agent-session.d.ts
+++ b/dist/core/agent-session.d.ts
@@ -104,6 +104,10 @@ export type AgentSessionEvent = Exclude<AgentEvent, {
 export type AgentSessionEventListener = (event: AgentSessionEvent) => void;
 export interface AgentSessionConfig {
     agent: Agent;
+    /** Stable catalog/scoped model before credential-specific endpoint resolution. */
+    selectedModel?: Model<any>;
+    /** Whether provider requests flow through ModelRuntime and consume prepared auth snapshots. */
+    usesModelRuntimeStream?: boolean;
     sessionManager: SessionManager;
     settingsManager: SettingsManager;
     cwd: string;
@@ -158,6 +162,8 @@ export interface PromptOptions {
     streamingBehavior?: "steer" | "followUp";
     /** Source of input for extension input event handlers. Defaults to "interactive". */
     source?: InputSource;
+    /** Transform the canonical expanded text immediately before it is delivered. */
+    transformExpandedText?: (text: string) => string;
     /** Internal hook used by RPC mode to observe prompt preflight acceptance or rejection. */
     preflightResult?: (success: boolean) => void;
 }
@@ -200,6 +206,8 @@ export declare class AgentSession {
     private _unsubscribeAgent?;
     private _eventListeners;
     private _isAgentRunActive;
+    private _requestRequiresReconstructionFit;
+    private _agentRunAbortRequested;
     private _agentRunAbortRequested;
     private _idleWaitPromise;
     private _resolveIdleWait;
@@ -248,6 +256,8 @@ export declare class AgentSession {
     private _extensionErrorListener?;
     private _extensionErrorUnsubscriber?;
     private _modelRuntime;
+    private _selectedModel;
+    private _usesModelRuntimeStream;
     private _cacheWarmer?;
     private _toolRegistry;
     private _toolDefinitions;
@@ -260,6 +270,8 @@ export declare class AgentSession {
     get modelRuntime(): ModelRuntime;
     private _getRequiredRequestAuth;
     private _getSummarizationRequestAuth;
+    private _refreshRequestModelContext;
+    private _prepareCompactionRequestAuth;
     /**
      * Install tool hooks once on the Agent instance.
      *
@@ -401,6 +413,7 @@ export declare class AgentSession {
     /** Restore the active tool loadout declared by the session transcript, if it declares one. */
     private _restoreToolsFromTranscript;
     private _runAgentPrompt;
+    private _prepareNewTurn;
     private _handlePostAgentRun;
     private _runBeforeSettleBoundary;
     private _runInputHandlers;
@@ -414,7 +427,7 @@ export declare class AgentSession {
      * @throws Error if streaming and no streamingBehavior specified
      * @throws Error if no model selected or no API key available (when not streaming)
      */
-    prompt(text: string, options?: PromptOptions): Promise<void>;
+    prompt(text: string, options?: PromptOptions): Promise<string | undefined>;
     private _tryExecuteExtensionCommand;
     /**
      * Expand skill commands (/skill:name args) to their full content.
@@ -432,9 +445,7 @@ export declare class AgentSession {
      * @param options Input source; defaults to interactive
      * @throws Error if text is an extension command
      */
-    steer(text: string, images?: ImageContent[], options?: {
-        source?: InputSource;
-    }): Promise<void>;
+    steer(text: string, images?: ImageContent[], transformExpandedText?: (text: string) => string): Promise<string>;
     /**
      * Queue a follow-up message to be processed after the agent finishes.
      * Delivered only when agent has no more tool calls or steering messages.
diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index 36fdb31c121d1121cebc51c6f0d61d09cf07bae0..e56d3dd0d9389198da4c98e5fab8fcd7ee6ba022 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -24,7 +24,7 @@ import { normalizeToolResultImages } from "../utils/tool-result-images.js";
 import { formatNoApiKeyFoundMessage, formatNoModelSelectedMessage } from "./auth-guidance.js";
 import { executeBashWithOperations } from "./bash-executor.js";
 import { generateBugReportSummary } from "./bug-report.js";
-import { calculateContextTokens, collectEntriesForBranchSummary, compact, estimateContextTokens, estimateProjectedContextTokens, estimateTokens, generateBranchSummary, prepareCompaction, shouldCompact, } from "./compaction/index.js";
+import { calculateContextTokens, collectEntriesForBranchSummary, compact, estimateContextTokens, estimatePortableCompactionRequestOverheadTokens, estimateProjectedContextTokens, estimateTokens, generateBranchSummary, prepareCompaction, selectCompactionMechanism, shouldCompact } from "./compaction/index.js";
 import { DEFAULT_THINKING_LEVEL, THINKING_LEVEL_OPTIONS } from "./defaults.js";
 import { exportSessionToHtml } from "./export-html/index.js";
 import { createToolHtmlRenderer } from "./export-html/tool-renderer.js";
@@ -32,9 +32,10 @@ import { ExtensionRunner, wrapRegisteredTools, } from "./extensions/index.js";
 import { emitSessionShutdownEvent } from "./extensions/runner.js";
 import { convertToLlm } from "./messages.js";
 import { ModelRegistry } from "./model-registry.js";
+import { mergeProviderAttributionHeaders } from "./provider-attribution.js";
 import { expandPromptTemplate } from "./prompt-templates.js";
 import { exportSessionToJsonl } from "./session-export.js";
-import { getLatestCompactionEntry, SessionManager, } from "./session-manager.js";
+import { estimateReconstructionMessageTokens, getLatestCompactionEntry, getNativeCredentialFingerprint, SessionManager, withNativeCredentialFingerprint, withPreparedProviderHeaders } from "./session-manager.js";
 import { createSyntheticSourceInfo } from "./source-info.js";
 import { buildSystemPrompt, buildSystemPromptSections, diffSystemPromptSections, normalizeBuildSystemPromptOptions, } from "./system-prompt.js";
 import { createLocalBashOperations } from "./tools/bash.js";
@@ -71,6 +72,45 @@ function estimateMessagesTokens(messages) {
     }
     return tokens;
 }
+function estimateRequestOverheadTokens(systemPrompt, tools, messages) {
+    let toolSchema = "";
+    try {
+        toolSchema = JSON.stringify(tools ?? []);
+    }
+    catch {
+        // Cyclic extension metadata is not part of the provider-facing JSON schema.
+    }
+    const estimateTextTokens = (text) => Math.max(text.length, Math.ceil(new TextEncoder().encode(text).length / 2));
+    const requestMessages = Array.isArray(messages) ? messages : [messages];
+    return estimateTextTokens(systemPrompt ?? "") + estimateTextTokens(toolSchema) + requestMessages.reduce((total, message) => total + estimateReconstructionMessageTokens(message), 0);
+}
+function isRequestTurnStart(message) {
+    return message.role === "user" || message.role === "custom" || message.role === "bashExecution" ||
+        message.role === "branchSummary" || message.role === "compactionSummary";
+}
+function fitMessagesToRequestBudget(messages, model, systemPrompt, tools) {
+    if (!model || !Number.isFinite(model.contextWindow))
+        return messages;
+    const units = [];
+    const lastAssistantIndex = messages.findLastIndex((message) => message.role === "assistant");
+    const mandatorySuffixIndex = messages.findIndex((message, index) => index > lastAssistantIndex && (message.role === "user" || message.role === "custom"));
+    for (const [index, message] of messages.entries()) {
+        const isMandatoryRequestSuffix = mandatorySuffixIndex >= 0 && index >= mandatorySuffixIndex;
+        if ((!isMandatoryRequestSuffix && isRequestTurnStart(message)) || index === mandatorySuffixIndex || units.length === 0)
+            units.push([]);
+        units[units.length - 1].push(message);
+    }
+    const framingTokens = estimateRequestOverheadTokens(systemPrompt, tools, []);
+    const safetyReserve = Math.ceil(Math.max(0, model.contextWindow) * 0.25);
+    const outputReserve = Number.isFinite(model.maxTokens) ? Math.max(0, model.maxTokens) : 0;
+    const inputBudget = Math.max(0, model.contextWindow - Math.max(safetyReserve, outputReserve + framingTokens));
+    const estimateUnitTokens = (unit) => unit.reduce((total, message) => total + estimateReconstructionMessageTokens(message), 0);
+    let totalTokens = units.reduce((total, unit) => total + estimateUnitTokens(unit), 0);
+    while (totalTokens > inputBudget && units.length > 1) {
+        totalTokens -= estimateUnitTokens(units.shift());
+    }
+    return units.flat();
+}
 // ============================================================================
 // AgentSession Class
 // ============================================================================
@@ -83,6 +123,7 @@ export class AgentSession {
     _unsubscribeAgent;
     _eventListeners = [];
     _isAgentRunActive = false;
+    _requestRequiresReconstructionFit = false;
     _agentRunAbortRequested = false;
     _idleWaitPromise;
     _resolveIdleWait;
@@ -136,6 +177,8 @@ export class AgentSession {
     _extensionErrorListener;
     _extensionErrorUnsubscriber;
     _modelRuntime;
+    _selectedModel;
+    _usesModelRuntimeStream;
     _cacheWarmer;
     // Tool registry for extension getTools/setTools
     _toolRegistry = new Map();
@@ -154,10 +197,11 @@ export class AgentSession {
         this._customTools = config.customTools ?? [];
         this._cwd = config.cwd;
         this._modelRuntime = config.modelRuntime;
+        this._selectedModel = config.selectedModel ?? config.agent.state.model;
+        this._usesModelRuntimeStream = config.usesModelRuntimeStream === true;
         this._cacheWarmer = config.cacheWarmer;
-        if (this._cacheWarmer) {
+        if (this._cacheWarmer)
             this._cacheWarmer.onWarmed = (entry) => this._emit({ type: "entry_appended", entry });
-        }
         this._extensionRunnerRef = config.extensionRunnerRef;
         this._initialActiveToolNames = config.initialActiveToolNames;
         this._allowedToolNames = config.allowedToolNames ? new Set(config.allowedToolNames) : undefined;
@@ -195,11 +239,13 @@ export class AgentSession {
             throw error;
         }
         if (result && (result.auth.apiKey || result.auth.headers)) {
-            const requestModel = result.auth.baseUrl ? { ...model, baseUrl: result.auth.baseUrl } : model;
+            const resolvedModel = { ...model, baseUrl: result.auth.baseUrl ?? model.baseUrl };
+            const headers = withoutDeletedHeaders(result.auth.headers);
+            const requestModel = withNativeCredentialFingerprint(resolvedModel, result.auth.apiKey, headers);
             return {
                 model: requestModel,
                 apiKey: result.auth.apiKey,
-                headers: withoutDeletedHeaders(result.auth.headers),
+                headers,
                 env: result.env,
             };
         }
@@ -216,15 +262,13 @@ export class AgentSession {
             return this._getRequiredRequestAuth(model, signal);
         }
         try {
-            const result = await this._modelRuntime.getAuth(model, { signal });
-            if (!result)
-                return { model };
-            const requestModel = result.auth.baseUrl ? { ...model, baseUrl: result.auth.baseUrl } : model;
+            const { model: requestModel, resolution } = await this._modelRuntime.prepareModelRequest(model, { signal });
+            const headers = withoutDeletedHeaders(resolution.auth.headers);
             return {
-                model: requestModel,
-                apiKey: result.auth.apiKey,
-                headers: withoutDeletedHeaders(result.auth.headers),
-                env: result.env,
+                model: withNativeCredentialFingerprint(requestModel, resolution.auth.apiKey, headers),
+                apiKey: resolution.auth.apiKey,
+                headers,
+                env: resolution.env,
             };
         }
         catch (error) {
@@ -233,6 +277,78 @@ export class AgentSession {
             return { model };
         }
     }
+    async _refreshRequestModelContext(onPreparedCompactionAuth, preserveCurrentNativeCredential = false) {
+        const currentModel = this.model;
+        if (!currentModel)
+            return false;
+        const selectedModel = this._selectedModel ?? currentModel;
+        let requestModel;
+        let apiKey;
+        let headers;
+        let env;
+        if (this._usesModelRuntimeStream) {
+            const prepared = await this._modelRuntime.prepareModelRequest(selectedModel);
+            apiKey = prepared.resolution.auth.apiKey;
+            headers = withoutDeletedHeaders(prepared.resolution.auth.headers);
+            env = prepared.resolution.env;
+            requestModel = withNativeCredentialFingerprint(prepared.model, apiKey, headers);
+        }
+        else {
+            const requestAuth = await this._getSummarizationRequestAuth(selectedModel);
+            requestModel = requestAuth.model;
+            apiKey = requestAuth.apiKey;
+            headers = requestAuth.headers;
+            env = requestAuth.env;
+        }
+        const requestIdentityChanged = currentModel.api !== requestModel.api ||
+            currentModel.provider !== requestModel.provider ||
+            currentModel.id !== requestModel.id ||
+            currentModel.baseUrl !== requestModel.baseUrl ||
+            (currentModel.compat?.compactionBaseUrl ?? currentModel.baseUrl) !==
+                (requestModel.compat?.compactionBaseUrl ?? requestModel.baseUrl) ||
+            currentModel.compat?.supportsCompaction !== requestModel.compat?.supportsCompaction;
+        if (preserveCurrentNativeCredential && !requestIdentityChanged &&
+            selectCompactionMechanism(currentModel) === "native")
+            return false;
+        if (selectCompactionMechanism(requestModel) === "native") {
+            let requestHeaders = mergeProviderAttributionHeaders(requestModel, this.settingsManager, this.sessionManager.getSessionId(), headers);
+            if (this._extensionRunner.hasHandlers("before_provider_headers"))
+                requestHeaders = await this._extensionRunner.emitBeforeProviderHeaders(requestHeaders ?? {});
+            requestModel = withNativeCredentialFingerprint(requestModel, apiKey, requestHeaders);
+            if (this._usesModelRuntimeStream) {
+                requestModel = withPreparedProviderHeaders(requestModel, requestHeaders);
+            }
+            onPreparedCompactionAuth?.({
+                model: requestModel,
+                apiKey,
+                headers,
+                env,
+                nativeRequestHeaders: requestHeaders,
+            });
+        }
+        const identityChanged = requestIdentityChanged ||
+            getNativeCredentialFingerprint(currentModel) !==
+                getNativeCredentialFingerprint(requestModel);
+        this.agent.state.model = requestModel;
+        const projectedContext = this.sessionManager.buildSessionProjection(requestModel);
+        this._requestRequiresReconstructionFit = projectedContext.reconstruction !== undefined;
+        if (identityChanged) {
+            this.agent.state.messages = projectedContext.messages;
+        }
+        return identityChanged;
+    }
+    async _prepareCompactionRequestAuth(model, signal) {
+        const requestAuth = await this._getSummarizationRequestAuth(model, signal);
+        let nativeRequestHeaders;
+        if (selectCompactionMechanism(requestAuth.model) === "native") {
+            nativeRequestHeaders = mergeProviderAttributionHeaders(requestAuth.model, this.settingsManager, this.sessionManager.getSessionId(), requestAuth.headers);
+            if (this._extensionRunner.hasHandlers("before_provider_headers")) {
+                nativeRequestHeaders = await this._extensionRunner.emitBeforeProviderHeaders(nativeRequestHeaders ?? {});
+            }
+            withNativeCredentialFingerprint(requestAuth.model, requestAuth.apiKey, nativeRequestHeaders);
+        }
+        return { ...requestAuth, nativeRequestHeaders };
+    }
     /**
      * Install tool hooks once on the Agent instance.
      *
@@ -294,24 +410,26 @@ export class AgentSession {
             };
         };
     }
-    async _compactBeforeNextAssistantResponse(context) {
+    async _compactBeforeNextAssistantResponse(context, preparedCompactionAuth) {
         const model = this.model;
         const settings = this.settingsManager.getCompactionSettings(model);
-        const projection = this.sessionManager.buildSessionProjection();
+        const projection = this.sessionManager.buildSessionProjection(this.model);
         if (!model ||
             model.contextWindow <= 0 ||
             !shouldCompact(estimateProjectedContextTokens(projection, this.sessionManager.getBranch()).tokens, model.contextWindow, settings)) {
             return { ...context, messages: projection.messages };
         }
-        await this._runAutoCompaction("threshold", false);
-        return { ...context, messages: this.sessionManager.buildSessionProjection().messages };
+        await this._runAutoCompaction("threshold", false, preparedCompactionAuth, true);
+        return { ...context, messages: this.sessionManager.buildSessionProjection(this.model).messages };
     }
     _installAgentRequestProjection() {
         const previousPrepareRequest = this.agent.prepareRequest;
         this.agent.prepareRequest = async (request, signal) => {
             const canonicalContext = {
                 ...request.context,
-                messages: this.sessionManager.buildSessionProjection().messages,
+                // Agent state starts from the canonical projection; retain this request snapshot
+                // so user and queued messages that have not settled into the session tree are not lost.
+                messages: request.context.messages,
                 // Messages declare the provider-visible loadout; context.tools keeps executable implementations.
                 tools: this.agent.state.tools.slice(),
             };
@@ -377,15 +495,18 @@ export class AgentSession {
         };
     }
     _installAgentNextTurnRefresh() {
+        const previousTransformContext = this.agent.transformContext;
         const previousPrepareNextTurnWithContext = this.agent.prepareNextTurnWithContext ??
             (this.agent.prepareNextTurn
                 ? async (_turn, signal) => await this.agent.prepareNextTurn?.(signal)
                 : undefined);
         this.agent.prepareNextTurnWithContext = async (turn, signal) => {
+            let preparedCompactionAuth;
+            await this._refreshRequestModelContext((prepared) => { preparedCompactionAuth = prepared; });
             const context = await this._compactBeforeNextAssistantResponse({
                 ...turn.context,
-                messages: this.sessionManager.buildSessionProjection().messages,
-            });
+                messages: this.sessionManager.buildSessionProjection(this.model).messages,
+            }, preparedCompactionAuth);
             const previousSnapshot = await previousPrepareNextTurnWithContext?.({ ...turn, context }, signal);
             const nextContext = previousSnapshot?.context ?? context;
             const runOptions = this._runSystemPromptOptions ?? this._baseSystemPromptOptions;
@@ -411,12 +532,18 @@ export class AgentSession {
                 thinkingLevel: this.agent.state.thinkingLevel,
             };
         };
+        this.agent.transformContext = async (messages, signal) => {
+            const transformed = previousTransformContext ? await previousTransformContext(messages, signal) : messages;
+            return this._requestRequiresReconstructionFit
+                ? fitMessagesToRequestBudget(transformed, this.model, this.systemPrompt, this.agent.state.tools)
+                : transformed;
+        };
     }
     // =========================================================================
     // Event Subscription
     // =========================================================================
-    _refreshFinalizedContext() {
-        const projection = this.sessionManager.buildSessionProjection();
+    _refreshFinalizedContext(targetModel = this.model) {
+        const projection = this.sessionManager.buildSessionProjection(targetModel);
         for (const entry of projection.entries) {
             for (const message of entry.messages)
                 this._entryIdsByMessage.set(message, entry.sourceEntry.id);
@@ -438,7 +565,7 @@ export class AgentSession {
                     entryId = manager.appendContextEdit(draft.targetId, draft.replacement);
                     break;
                 case "compaction": {
-                    const tokensBefore = estimateProjectedContextTokens(manager.buildSessionProjection(), manager.getBranch()).tokens;
+                    const tokensBefore = estimateProjectedContextTokens(manager.buildSessionProjection(this.model), manager.getBranch()).tokens;
                     entryId = manager.appendCompaction(draft.summary, draft.firstKeptEntryId, tokensBefore, draft.details, true, draft.usage);
                     break;
                 }
@@ -461,7 +588,7 @@ export class AgentSession {
         return [...this.agent.peekQueuedMessages(), ...this._pendingCustomMessages];
     }
     _buildBoundaryContext(drafts, boundary) {
-        const projection = this._createBoundaryPreviewManager(drafts).buildSessionProjection();
+        const projection = this._createBoundaryPreviewManager(drafts).buildSessionProjection(this.model);
         const pendingMessages = this._getPendingBoundaryMessages();
         const llmMessages = convertToLlm(projection.messages);
         const finalRole = llmMessages[llmMessages.length - 1]?.role;
@@ -651,7 +778,7 @@ export class AgentSession {
         const messageIndex = this.agent.state.messages.indexOf(message);
         if (messageIndex < 0)
             return undefined;
-        const projection = this.sessionManager.buildSessionProjection();
+        const projection = this.sessionManager.buildSessionProjection(this.model);
         let projectedIndex = 0;
         for (const entry of projection.entries) {
             for (let i = 0; i < entry.messages.length; i++) {
@@ -1060,7 +1187,7 @@ export class AgentSession {
     }
     /** Restore the active tool loadout declared by the session transcript, if it declares one. */
     _restoreToolsFromTranscript() {
-        const current = getCurrentSystemMessage(this.sessionManager.buildSessionContext().messages);
+        const current = getCurrentSystemMessage(this.sessionManager.buildSessionProjection(this.model).messages);
         if (!current)
             return;
         const toolNames = (current.toolsAdded ?? [])
@@ -1079,6 +1206,10 @@ export class AgentSession {
         this._agentRunAbortRequested = false;
         this._isAgentRunActive = true;
         try {
+            const requestOverheadTokens = estimateRequestOverheadTokens(this.systemPrompt, this.agent.state.tools, messages);
+            const projectedContext = this.sessionManager.buildSessionProjection(this.model, { requestOverheadTokens });
+            this._requestRequiresReconstructionFit = projectedContext.reconstruction !== undefined;
+            this.agent.state.messages = projectedContext.messages;
             await this.agent.prompt(messages);
             while (!this._agentRunAbortRequested) {
                 if (await this._handlePostAgentRun()) {
@@ -1103,6 +1234,16 @@ export class AgentSession {
             await this._emitAgentSettled();
         }
     }
+    async _prepareNewTurn() {
+        let preparedCompactionAuth;
+        await this._refreshRequestModelContext((prepared) => {
+            preparedCompactionAuth = prepared;
+        });
+        const lastAssistant = this._findLastAssistantMessage();
+        if (lastAssistant) {
+            await this._checkCompaction(lastAssistant, false, [], true, preparedCompactionAuth);
+        }
+    }
     async _handlePostAgentRun() {
         const message = this._lastAssistantMessage;
         const toolResults = this._lastAssistantToolResults;
@@ -1132,7 +1273,8 @@ export class AgentSession {
             });
             this._retryAttempt = 0;
         }
-        if (await this._checkCompaction(message, true, toolResults)) {
+        const hasQueuedMessages = this.agent.hasQueuedMessages();
+        if (await this._checkCompaction(message, true, toolResults, hasQueuedMessages)) {
             return !this._agentRunAbortRequested;
         }
         // The low-level loop drains both queues before agent_end. Messages queued by
@@ -1213,32 +1355,31 @@ export class AgentSession {
         const preflightResult = options?.preflightResult;
         let messages;
         try {
-            // Handle extension commands first (execute immediately, even during streaming)
-            // Extension commands manage their own LLM interaction via pi.sendMessage()
-            if (expandPromptTemplates && text.startsWith("/")) {
-                const handled = await this._tryExecuteExtensionCommand(text);
+            const processedInput = await this._runInputHandlers(text, options?.images, options?.source ?? "interactive", this.isStreaming ? options?.streamingBehavior : undefined);
+            if (!processedInput) {
+                preflightResult?.(true);
+                return;
+            }
+            let { text: currentText, images: currentImages } = processedInput;
+            // Transformed input decides command dispatch so extensions can stop or rewrite active work.
+            if (expandPromptTemplates && currentText.startsWith("/")) {
+                const handled = await this._tryExecuteExtensionCommand(currentText);
                 if (handled) {
-                    // Extension command executed, no prompt to send
                     preflightResult?.(true);
                     return;
                 }
             }
-            if (this._compactionAbortController !== undefined) {
+            if (this._compactionAbortController !== undefined)
                 throw new Error("Cannot submit a prompt while compaction is in progress. Wait for compaction to finish and retry.");
-            }
-            // Emit input event for extension interception (before skill/template expansion)
-            const processedInput = await this._runInputHandlers(text, options?.images, options?.source ?? "interactive", this.isStreaming ? options?.streamingBehavior : undefined);
-            if (!processedInput) {
-                preflightResult?.(true);
-                return;
-            }
-            const { text: currentText, images: currentImages } = processedInput;
             // Expand skill commands (/skill:name args) and prompt templates (/template args)
             let expandedText = currentText;
             if (expandPromptTemplates) {
                 expandedText = this._expandSkillCommand(expandedText);
                 expandedText = expandPromptTemplate(expandedText, [...this.promptTemplates]);
             }
+            if (options?.transformExpandedText) {
+                expandedText = options.transformExpandedText(expandedText);
+            }
             // If streaming, queue via steer() or followUp() based on option
             if (this.isStreaming) {
                 if (!options?.streamingBehavior) {
@@ -1251,7 +1392,7 @@ export class AgentSession {
                     await this._queueSteer(expandedText, currentImages);
                 }
                 preflightResult?.(true);
-                return;
+                return expandedText;
             }
             // Flush any pending bash and custom messages before the new prompt
             this._flushPendingBashMessages();
@@ -1271,13 +1412,7 @@ export class AgentSession {
                 }
                 throw new Error(formatNoApiKeyFoundMessage(this.model.provider));
             }
-            // Check if we need to compact before sending (catches aborted responses).
-            // The user's new prompt is sent below, so do not call agent.continue() here.
-            const lastAssistant = this._findLastAssistantMessage();
-            if (lastAssistant) {
-                await this._checkCompaction(lastAssistant, false);
-            }
-            // Emit before_agent_start before normalizing images so extension-driven model
+            // Emit before_agent_start before preparing the request so extension-driven model
             // selection determines the resize profile used for the request and history.
             const selectedToolsBefore = this._baseSystemPromptOptions.selectedTools;
             const result = await this._extensionRunner.emitBeforeAgentStart(expandedText, currentImages, this._baseSystemPromptOptions);
@@ -1288,6 +1423,7 @@ export class AgentSession {
                 result.systemPromptOptions.selectedTools.some((name, index) => name !== selectedToolsBefore[index]);
             if (!handlerEditedTools)
                 result.systemPromptOptions.selectedTools = this.getActiveToolNames();
+            await this._prepareNewTurn();
             const normalized = await this._normalizePromptImages(currentImages);
             const userText = normalized.hints.length > 0 ? `${expandedText}\n\n${normalized.hints.join("\n")}` : expandedText;
             // Build messages only after hooks and image normalization have completed.
@@ -1387,7 +1523,7 @@ export class AgentSession {
             return text; // Return original on error
         }
     }
-    async _queueUserInput(text, images, behavior, source) {
+    async _queueUserInput(text, images, behavior, source, transformExpandedText) {
         if (text.startsWith("/")) {
             this._throwIfExtensionCommand(text);
         }
@@ -1396,12 +1532,15 @@ export class AgentSession {
             return;
         let expandedText = this._expandSkillCommand(processedInput.text);
         expandedText = expandPromptTemplate(expandedText, [...this.promptTemplates]);
+        if (transformExpandedText)
+            expandedText = transformExpandedText(expandedText);
         if (behavior === "steer") {
             await this._queueSteer(expandedText, processedInput.images);
         }
         else {
             await this._queueFollowUp(expandedText, processedInput.images);
         }
+        return expandedText;
     }
     /**
      * Queue a steering message while the agent is running.
@@ -1413,7 +1552,8 @@ export class AgentSession {
      * @throws Error if text is an extension command
      */
     async steer(text, images, options) {
-        await this._queueUserInput(text, images, "steer", options?.source ?? "interactive");
+        const normalizedOptions = typeof options === "function" ? { transformExpandedText: options } : options;
+        return this._queueUserInput(text, images, "steer", normalizedOptions?.source ?? "interactive", normalizedOptions?.transformExpandedText);
     }
     /**
      * Queue a follow-up message to be processed after the agent finishes.
@@ -1500,11 +1640,16 @@ export class AgentSession {
             }
         }
         else if (options?.triggerTurn) {
+            const run = async () => {
+                await this._prepareNewTurn();
+                await this._runAgentPrompt(appMessage);
+            };
             if (this._isEmittingAgentSettled) {
-                this._deferredSettledActions.push(async () => await this._runAgentPrompt(appMessage));
+                this._deferredSettledActions.push(run);
                 return;
             }
-            await this._runAgentPrompt(appMessage);
+            await run();
+            return;
         }
         else if (this.isStreaming) {
             // Appending now would put the message between an assistant tool call and its
@@ -1643,13 +1788,13 @@ export class AgentSession {
      * @throws Error if no auth is configured for the model
      */
     async setModel(model, options = {}) {
-        if (!(await this._modelRuntime.checkAuth(model.provider))) {
-            throw new Error(`No API key for ${model.provider}/${model.id}`);
-        }
+        const { model: requestModel } = await this._getRequiredRequestAuth(model);
         const previousModel = this.model;
         const thinkingLevel = this._getThinkingLevelForModelSwitch(model);
-        this.agent.state.model = model;
+        this._selectedModel = model;
+        this.agent.state.model = requestModel;
         this.sessionManager.appendModelChange(model.provider, model.id);
+        this._refreshFinalizedContext(requestModel);
         if (options.persist) {
             this.settingsManager.setDefaultModelAndProvider(model.provider, model.id);
             this._addPersistedDefaultToNonEmptyScope(model);
@@ -1658,7 +1803,7 @@ export class AgentSession {
         // Per-model thinking level overrides take priority over the global default.
         // Model persistence does not implicitly rewrite the global thinking default.
         this.setThinkingLevel(thinkingLevel);
-        await this._emitModelSelect(model, previousModel, "set");
+        await this._emitModelSelect(requestModel, previousModel, "set");
     }
     _addPersistedDefaultToNonEmptyScope(model) {
         if (this._scopedModels.length === 0)
@@ -1698,10 +1843,13 @@ export class AgentSession {
         const len = scopedModels.length;
         const nextIndex = direction === "forward" ? (currentIndex + 1) % len : (currentIndex - 1 + len) % len;
         const next = scopedModels[nextIndex];
+        const { model: requestModel } = await this._getRequiredRequestAuth(next.model);
         const thinkingLevel = this._getThinkingLevelForModelSwitch(next.model, next.thinkingLevel);
         // Apply model
-        this.agent.state.model = next.model;
+        this._selectedModel = next.model;
+        this.agent.state.model = requestModel;
         this.sessionManager.appendModelChange(next.model.provider, next.model.id);
+        this._refreshFinalizedContext(requestModel);
         if (options.persist) {
             this.settingsManager.setDefaultModelAndProvider(next.model.provider, next.model.id);
             this._addPersistedDefaultToNonEmptyScope(next.model);
@@ -1712,8 +1860,8 @@ export class AgentSession {
         // setThinkingLevel clamps to model capabilities.
         // Model persistence does not implicitly rewrite the global thinking default.
         this.setThinkingLevel(thinkingLevel);
-        await this._emitModelSelect(next.model, currentModel, "cycle");
-        return { model: next.model, thinkingLevel: this.thinkingLevel, isScoped: true };
+        await this._emitModelSelect(requestModel, currentModel, "cycle");
+        return { model: requestModel, thinkingLevel: this.thinkingLevel, isScoped: true };
     }
     async _cycleAvailableModel(direction, options) {
         const availableModels = this._modelRuntime.getAvailableSnapshot();
@@ -1726,9 +1874,12 @@ export class AgentSession {
         const len = availableModels.length;
         const nextIndex = direction === "forward" ? (currentIndex + 1) % len : (currentIndex - 1 + len) % len;
         const nextModel = availableModels[nextIndex];
+        const { model: requestModel } = await this._getRequiredRequestAuth(nextModel);
         const thinkingLevel = this._getThinkingLevelForModelSwitch(nextModel);
-        this.agent.state.model = nextModel;
+        this._selectedModel = nextModel;
+        this.agent.state.model = requestModel;
         this.sessionManager.appendModelChange(nextModel.provider, nextModel.id);
+        this._refreshFinalizedContext(requestModel);
         if (options.persist) {
             this.settingsManager.setDefaultModelAndProvider(nextModel.provider, nextModel.id);
             this._addPersistedDefaultToNonEmptyScope(nextModel);
@@ -1736,8 +1887,8 @@ export class AgentSession {
         // Apply thinking level for the new model.
         // Model persistence does not implicitly rewrite the global thinking default.
         this.setThinkingLevel(thinkingLevel);
-        await this._emitModelSelect(nextModel, currentModel, "cycle");
-        return { model: nextModel, thinkingLevel: this.thinkingLevel, isScoped: false };
+        await this._emitModelSelect(requestModel, currentModel, "cycle");
+        return { model: requestModel, thinkingLevel: this.thinkingLevel, isScoped: false };
     }
     // =========================================================================
     // Thinking Level Management
@@ -1840,8 +1991,38 @@ export class AgentSession {
     // Compaction
     // =========================================================================
     /** Generate Pi's built-in compaction summary for manual and automatic compaction. */
-    async _runDefaultCompaction(preparation, requestModel, apiKey, headers, customInstructions, signal, env, reason) {
-        return compact(preparation, requestModel, apiKey, headers, customInstructions, signal, this.thinkingLevel, this.agent.streamFunction, env, this.settingsManager.getRetrySettings(), this._summarizationRetryCallbacks({ source: "compaction", reason }), undefined);
+    async _runDefaultCompaction(preparation, requestModel, apiKey, headers, customInstructions, signal, env, reason, willRetry, nativeRequestHeaders) {
+        const transformPreparation = this.agent.transformCompactionContext || this.agent.transformContext
+            ? async (candidate, mechanism) => {
+                const referenceMessages = willRetry
+                    ? (this._compactionReferenceMessages ?? this.agent.state.messages)
+                    : candidate.messagesForNativeCompaction;
+                const transformMessages = (messages) => messages.length === 0 || (mechanism !== "native" && !this.agent.transformCompactionContext)
+                    ? messages
+                    : this.agent.transformCompactionContext ? this.agent.transformCompactionContext(messages, referenceMessages, { willRetry }, signal) : this.agent.transformContext(messages, signal);
+                return mechanism === "native"
+                    ? {
+                        ...candidate,
+                        messagesForNativeCompaction: await transformMessages(candidate.messagesForNativeCompaction),
+                    }
+                    : {
+                        ...candidate,
+                        messagesToSummarize: await transformMessages(candidate.messagesToSummarize),
+                        turnPrefixMessages: await transformMessages(candidate.turnPrefixMessages),
+                    };
+            }
+            : undefined;
+        let nativeRequestOptions;
+        if (selectCompactionMechanism(requestModel) === "native") {
+            const httpIdleTimeoutMs = this.settingsManager.getHttpIdleTimeoutMs();
+            nativeRequestOptions = {
+                headers: nativeRequestHeaders,
+                timeoutMs: this.settingsManager.getProviderRetrySettings().timeoutMs ?? (httpIdleTimeoutMs === 0 ? 2147483647 : httpIdleTimeoutMs),
+                onPayload: this.agent.onPayload,
+                onResponse: this.agent.onResponse, convertMessages: async (messages) => this.agent.convertToLlm(messages),
+            };
+        }
+        return compact(preparation, requestModel, apiKey, headers, customInstructions, signal, this.thinkingLevel, this.agent.streamFunction, env, this.settingsManager.getRetrySettings(), this._summarizationRetryCallbacks({ source: "compaction", reason }), this.sessionManager.getSessionId(), this.systemPrompt, transformPreparation, nativeRequestOptions);
     }
     _clearManualCompactionState() {
         this._compactionAbortController = undefined;
@@ -1873,10 +2054,24 @@ export class AgentSession {
             if (!model) {
                 throw new Error(formatNoModelSelectedMessage());
             }
-            const settings = this.settingsManager.getCompactionSettings(model);
-            const { model: requestModel, apiKey, headers, env, } = await this._getSummarizationRequestAuth(model, this._compactionAbortController.signal);
+            const { model: requestModel, apiKey, headers, env, nativeRequestHeaders } = await this._prepareCompactionRequestAuth(this._selectedModel ?? model, this._compactionAbortController.signal);
+            this.agent.state.model = requestModel;
+            const nativeInstructions = customInstructions?.trim()
+                ? `${this.systemPrompt}\n\nAdditional compaction instructions:\n${customInstructions.trim()}`
+                : this.systemPrompt;
+            const nativeRequestOverheadTokens = estimateRequestOverheadTokens(nativeInstructions, [], []);
+            const requestOverheadTokens = selectCompactionMechanism(requestModel) === "native"
+                ? nativeRequestOverheadTokens
+                : estimatePortableCompactionRequestOverheadTokens(customInstructions);
+            const projection = this.sessionManager.buildSessionProjection(requestModel, { requestOverheadTokens });
+            this.agent.state.messages = projection.messages;
+            this._requestRequiresReconstructionFit = projection.reconstruction !== undefined;
             const pathEntries = this.sessionManager.getBranch();
-            const preparation = prepareCompaction(pathEntries, settings);
+            const settings = this.settingsManager.getCompactionSettings(requestModel);
+            const preparation = prepareCompaction(pathEntries, settings, requestModel, {
+                nativeRequestOverheadTokens,
+                portableCustomInstructions: customInstructions,
+            });
             if (!preparation) {
                 // Check why we can't compact
                 const lastEntry = pathEntries[pathEntries.length - 1];
@@ -1920,7 +2115,7 @@ export class AgentSession {
             }
             else {
                 // Shared default summary generator, also used by automatic compaction.
-                const result = await this._runDefaultCompaction(preparation, requestModel, apiKey, headers, customInstructions, this._compactionAbortController.signal, env, "manual");
+                const result = await this._runDefaultCompaction(preparation, requestModel, apiKey, headers, customInstructions, this._compactionAbortController.signal, env, "manual", false, nativeRequestHeaders);
                 summary = result.summary;
                 firstKeptEntryId = result.firstKeptEntryId;
                 tokensBefore = result.tokensBefore;
@@ -1930,12 +2125,11 @@ export class AgentSession {
             if (this._compactionAbortController.signal.aborted) {
                 throw new Error("Compaction cancelled");
             }
-            this.sessionManager.appendCompaction(summary, firstKeptEntryId, tokensBefore, details, fromExtension, usage);
-            const newEntries = this.sessionManager.getEntries();
-            this._refreshFinalizedContext();
-            const estimatedTokensAfter = estimateMessagesTokens(this.sessionManager.buildSessionProjection().messages);
+            const savedCompactionEntryId = this.sessionManager.appendCompaction(summary, firstKeptEntryId, tokensBefore, details, fromExtension, usage, "manual");
+            this._refreshFinalizedContext(requestModel);
+            const estimatedTokensAfter = estimateMessagesTokens(this.sessionManager.buildSessionProjection(requestModel).messages);
             // Get the saved compaction entry for the extension event
-            const savedCompactionEntry = newEntries.find((e) => e.type === "compaction" && e.summary === summary);
+            const savedCompactionEntry = this.sessionManager.getEntry(savedCompactionEntryId);
             if (this._extensionRunner && savedCompactionEntry) {
                 await this._extensionRunner.emit({
                     type: "session_compact",
@@ -1946,6 +2140,7 @@ export class AgentSession {
                 });
             }
             const compactionResult = {
+                entryId: savedCompactionEntryId,
                 summary,
                 firstKeptEntryId,
                 tokensBefore,
@@ -2024,7 +2219,7 @@ export class AgentSession {
      * @param skipAbortedCheck If false, include aborted messages (for pre-prompt check). Default: true
      * @returns Whether the post-run loop should call `agent.continue()` for overflow recovery or queued messages
      */
-    async _checkCompaction(assistantMessage, skipAbortedCheck = true, toolResults = []) {
+    async _checkCompaction(assistantMessage, skipAbortedCheck = true, toolResults = [], hasFollowUpCall = true, preparedCompactionAuth) {
         const settings = this.settingsManager.getCompactionSettings(this.model);
         if (!settings.enabled)
             return false;
@@ -2048,7 +2243,7 @@ export class AgentSession {
         // Automatic cases 1 and 2: context overflow.
         // A length stop is recoverable when output ended below the model's original desired limit,
         // independent of the configured context size or any context-clamped provider request limit.
-        const currentProjection = this.sessionManager.buildSessionProjection();
+        const currentProjection = this.sessionManager.buildSessionProjection(this.model);
         const assistantEntryId = this._findPersistedMessageEntryId(assistantMessage);
         const assistantIsProjected = assistantEntryId === undefined ||
             currentProjection.entries.some((entry) => entry.sourceEntry.id === assistantEntryId &&
@@ -2074,7 +2269,10 @@ export class AgentSession {
             // Case 2: the response completed successfully. Compact, but do not retry because
             // agent.continue() cannot continue from a completed assistant response.
             if (!willRetry) {
-                return await this._runAutoCompaction("overflow", false);
+                if (!hasFollowUpCall) {
+                    return false;
+                }
+                return await this._runAutoCompaction("overflow", false, preparedCompactionAuth);
             }
             if (this._overflowRecoveryAttempted) {
                 const errorMessage = contextOverflow
@@ -2099,8 +2297,9 @@ export class AgentSession {
             }
             // Persistently omit the selected final attempt before post-run recovery compaction.
             this._overflowRecoveryAttempted = true;
+            this._compactionReferenceMessages = this.agent.state.messages.slice();
             this._omitRecoveryAttempt(assistantMessage, toolResults);
-            return await this._runAutoCompaction("overflow", willRetry);
+            return await this._runAutoCompaction("overflow", willRetry, preparedCompactionAuth);
         }
         // Case 3: threshold compaction without retry.
         // For error messages or all-zero usage messages, estimate from the last valid response.
@@ -2134,8 +2333,8 @@ export class AgentSession {
         else {
             contextTokens = directContextTokens;
         }
-        if (shouldCompact(contextTokens, contextWindow, settings)) {
-            return await this._runAutoCompaction("threshold", false);
+        if (hasFollowUpCall && shouldCompact(contextTokens, contextWindow, settings)) {
+            return await this._runAutoCompaction("threshold", false, preparedCompactionAuth);
         }
         return false;
     }
@@ -2149,29 +2348,39 @@ export class AgentSession {
      * @param willRetry Whether to continue the interrupted turn after overflow compaction
      * @returns Whether the post-run loop should call `agent.continue()`
      */
-    async _runAutoCompaction(reason, willRetry) {
+    async _runAutoCompaction(reason, willRetry, preparedCompactionAuth, preservePreparedAuth = false) {
         const model = this.model;
-        const settings = this.settingsManager.getCompactionSettings(model);
         let abortController;
         let started = false;
         let fromExtension = false;
         let cancelledByExtension = false;
+        let preserveCompactionAuth = preservePreparedAuth;
+        let completedWithNativeCheckpoint = false;
         try {
             if (!model) {
                 return false;
             }
-            const pathEntries = this.sessionManager.getBranch();
-            const preparation = prepareCompaction(pathEntries, settings);
-            if (!preparation) {
-                return false;
-            }
             abortController = new AbortController();
             this._autoCompactionAbortController = abortController;
+            const { model: requestModel, apiKey, headers, env, nativeRequestHeaders } = preparedCompactionAuth ??
+                (await this._prepareCompactionRequestAuth(this._selectedModel ?? model, abortController.signal));
+            abortController.signal.throwIfAborted();
+            preserveCompactionAuth ||= selectCompactionMechanism(requestModel) === "native" && this._extensionRunner.hasHandlers("before_provider_headers");
+            this.agent.state.model = requestModel;
+            const nativeRequestOverheadTokens = estimateRequestOverheadTokens(this.systemPrompt, [], []);
+            const requestOverheadTokens = selectCompactionMechanism(requestModel) === "native" ? nativeRequestOverheadTokens : estimatePortableCompactionRequestOverheadTokens();
+            const projection = this.sessionManager.buildSessionProjection(requestModel, { requestOverheadTokens });
+            this.agent.state.messages = projection.messages;
+            this._requestRequiresReconstructionFit = projection.reconstruction !== undefined;
+            const pathEntries = this.sessionManager.getBranch();
+            const settings = this.settingsManager.getCompactionSettings(requestModel);
+            const preparation = prepareCompaction(pathEntries, settings, requestModel, {
+                nativeRequestOverheadTokens,
+            });
+            if (!preparation)
+                return false;
             started = true;
             this._emit({ type: "compaction_start", reason });
-            abortController.signal.throwIfAborted();
-            const { model: requestModel, apiKey, headers, env, } = await this._getSummarizationRequestAuth(model, abortController.signal);
-            abortController.signal.throwIfAborted();
             let extensionCompaction;
             if (this._extensionRunner.hasHandlers("session_before_compact")) {
                 const extensionResult = (await this._extensionRunner.emit({
@@ -2208,7 +2417,7 @@ export class AgentSession {
             }
             else {
                 // Shared default summary generator, also used by manual compaction.
-                const compactResult = await this._runDefaultCompaction(preparation, requestModel, apiKey, headers, undefined, abortController.signal, env, reason);
+                const compactResult = await this._runDefaultCompaction(preparation, requestModel, apiKey, headers, undefined, abortController.signal, env, reason, willRetry, nativeRequestHeaders);
                 summary = compactResult.summary;
                 firstKeptEntryId = compactResult.firstKeptEntryId;
                 tokensBefore = compactResult.tokensBefore;
@@ -2216,12 +2425,12 @@ export class AgentSession {
                 details = compactResult.details;
             }
             abortController.signal.throwIfAborted();
-            this.sessionManager.appendCompaction(summary, firstKeptEntryId, tokensBefore, details, fromExtension, usage);
-            const newEntries = this.sessionManager.getEntries();
-            this._refreshFinalizedContext();
-            const estimatedTokensAfter = estimateMessagesTokens(this.sessionManager.buildSessionProjection().messages);
+            completedWithNativeCheckpoint = details?.schemaVersion === 1 && details?.mechanism === "native";
+            const savedCompactionEntryId = this.sessionManager.appendCompaction(summary, firstKeptEntryId, tokensBefore, details, fromExtension, usage, reason);
+            this._refreshFinalizedContext(requestModel);
+            const estimatedTokensAfter = estimateMessagesTokens(this.sessionManager.buildSessionProjection(requestModel).messages);
             // Get the saved compaction entry for the extension event
-            const savedCompactionEntry = newEntries.find((e) => e.type === "compaction" && e.summary === summary);
+            const savedCompactionEntry = this.sessionManager.getEntry(savedCompactionEntryId);
             if (this._extensionRunner && savedCompactionEntry) {
                 await this._extensionRunner.emit({
                     type: "session_compact",
@@ -2232,6 +2441,7 @@ export class AgentSession {
                 });
             }
             const result = {
+                entryId: savedCompactionEntryId,
                 summary,
                 firstKeptEntryId,
                 tokensBefore,
@@ -2274,10 +2484,16 @@ export class AgentSession {
             return false;
         }
         finally {
-            if (this._autoCompactionAbortController === abortController) {
-                this._autoCompactionAbortController = undefined;
+            try {
+                if (this.model && !preserveCompactionAuth)
+                    await this._refreshRequestModelContext(undefined, completedWithNativeCheckpoint);
+            }
+            finally {
+                if (this._autoCompactionAbortController === abortController)
+                    this._autoCompactionAbortController = undefined;
+                this._compactionReferenceMessages = undefined;
+                this._resolveIdleWaitIfIdle();
             }
-            this._resolveIdleWaitIfIdle();
         }
     }
     /**
@@ -2369,7 +2585,7 @@ export class AgentSession {
         if (!refreshedModel || refreshedModel === currentModel) {
             return;
         }
-        this.agent.state.model = refreshedModel;
+        this._selectedModel = refreshedModel;
     }
     _bindExtensionCore(runner) {
         const getCommands = () => {
@@ -2923,7 +3139,7 @@ export class AgentSession {
             let summaryDetails;
             let summaryUsage;
             if (options.summarize && entriesToSummarize.length > 0 && !extensionSummary) {
-                const model = this.model;
+                const model = this._selectedModel ?? this.model;
                 const { model: requestModel, apiKey, headers, env } = await this._getSummarizationRequestAuth(model);
                 const branchSummarySettings = this.settingsManager.getBranchSummarySettings();
                 const result = await generateBranchSummary(entriesToSummarize, {
@@ -3105,7 +3321,7 @@ export class AgentSession {
         // After compaction, the last assistant usage reflects pre-compaction context size.
         // We can only trust usage from an assistant that responded after the latest compaction.
         // If no such assistant exists, context token count is unknown until the next LLM response.
-        const projection = this.sessionManager.buildSessionProjection();
+        const projection = this.sessionManager.buildSessionProjection(this.model);
         const branch = this.sessionManager.getBranch();
         const latestCompaction = getLatestCompactionEntry(branch);
         if (latestCompaction) {
diff --git a/dist/core/compaction/compaction.d.ts b/dist/core/compaction/compaction.d.ts
index 04416da43184fce4dc2fc33afeb920e2edddedc9..b90908774b8ba2116736f3ef76a1f9bb086abce4 100644
--- a/dist/core/compaction/compaction.d.ts
+++ b/dist/core/compaction/compaction.d.ts
@@ -6,7 +6,7 @@
  */
 import type { AgentMessage, StreamFn, ThinkingLevel } from "@earendil-works/pi-agent-core";
 import { type RetryCallbacks, type RetryPolicy } from "@earendil-works/pi-ai";
-import type { AssistantMessage, Model, SimpleStreamOptions, TranscriptContext, Usage } from "@earendil-works/pi-ai/compat";
+import type { AssistantMessage, Message, Model, SimpleStreamOptions, TranscriptContext, Usage } from "@earendil-works/pi-ai/compat";
 import { type SessionEntry, type SessionProjection } from "../session-manager.ts";
 import { type FileOperations } from "./utils.ts";
 /** Details stored in CompactionEntry.details for file tracking */
@@ -16,6 +16,8 @@ export interface CompactionDetails {
 }
 /** Result from compact() - SessionManager adds uuid/parentUuid when saving */
 export interface CompactionResult<T = unknown> {
+    /** Persisted compaction entry ID, populated by AgentSession lifecycle results. */
+    entryId?: string;
     summary: string;
     firstKeptEntryId: string;
     tokensBefore: number;
@@ -25,10 +27,12 @@ export interface CompactionResult<T = unknown> {
     /** Extension-specific data (e.g., ArtifactIndex, version markers for structured compaction) */
     details?: T;
 }
+export type NativeCompactionRequestOptions = Pick<SimpleStreamOptions, "headers" | "onPayload" | "onResponse" | "timeoutMs"> & { convertMessages?: (messages: AgentMessage[]) => Promise<Message[]> };
 export interface CompactionSettings {
     enabled: boolean;
     reserveTokens: number;
     keepRecentTokens: number;
+    thresholdPercent: number;
 }
 export declare const DEFAULT_COMPACTION_SETTINGS: CompactionSettings;
 /**
@@ -57,6 +61,7 @@ export declare function estimateProjectedContextTokens(projection: SessionProjec
  * Check if compaction should trigger based on context usage.
  */
 export declare function shouldCompact(contextTokens: number, contextWindow: number, settings: CompactionSettings): boolean;
+export declare function selectCompactionMechanism(model: Model<any>): "native" | "portable";
 /**
  * Estimate token count for a message using chars/4 heuristic.
  * This is conservative (overestimates tokens).
@@ -120,6 +125,8 @@ export interface CompactionPreparation {
     firstKeptEntryId: string;
     /** Messages that will be summarized and discarded */
     messagesToSummarize: AgentMessage[];
+    /** Complete active context used by a native replacement checkpoint. */
+    messagesForNativeCompaction: AgentMessage[];
     /** Messages that will be turned into turn prefix summary (if splitting) */
     turnPrefixMessages: AgentMessage[];
     /** Whether this is a split turn (cut point in middle of turn) */
@@ -131,8 +138,16 @@ export interface CompactionPreparation {
     fileOps: FileOperations;
     /** Compaction settions from settings.jsonl	*/
     settings: CompactionSettings;
+    /** Raw-history preparation used only when a declared Native endpoint is unavailable. */
+    portableFallback?: CompactionPreparation;
 }
-export declare function prepareCompaction(pathEntries: SessionEntry[], settings: CompactionSettings): CompactionPreparation | undefined;
+export interface CompactionPreparationOptions {
+    nativeRequestOverheadTokens?: number;
+    portableCustomInstructions?: string;
+}
+export type CompactionPreparationTransform = (preparation: CompactionPreparation, mechanism: "native" | "portable") => Promise<CompactionPreparation>;
+export declare function estimatePortableCompactionRequestOverheadTokens(customInstructions?: string, previousSummary?: string, messages?: AgentMessage[]): number;
+export declare function prepareCompaction(pathEntries: SessionEntry[], settings: CompactionSettings, model?: Model<any>, requestOptions?: CompactionPreparationOptions): CompactionPreparation | undefined;
 /**
  * Generate summaries for compaction using prepared data.
  * Returns CompactionResult - SessionManager adds uuid/parentUuid when saving.
@@ -141,5 +156,5 @@ export declare function prepareCompaction(pathEntries: SessionEntry[], settings:
  * @param customInstructions - Optional custom focus for the summary
  * @param sessionId - Optional routing session ID forwarded without enabling prompt caching
  */
-export declare function compact(preparation: CompactionPreparation, model: Model<any>, apiKey: string | undefined, headers?: Record<string, string>, customInstructions?: string, signal?: AbortSignal, thinkingLevel?: ThinkingLevel, streamFn?: StreamFn, env?: Record<string, string>, retry?: RetryPolicy, callbacks?: RetryCallbacks, sessionId?: string): Promise<CompactionResult>;
+export declare function compact(preparation: CompactionPreparation, model: Model<any>, apiKey: string | undefined, headers?: Record<string, string>, customInstructions?: string, signal?: AbortSignal, thinkingLevel?: ThinkingLevel, streamFn?: StreamFn, env?: Record<string, string>, retry?: RetryPolicy, callbacks?: RetryCallbacks, sessionId?: string, systemPrompt?: string, transformPreparation?: CompactionPreparationTransform, nativeRequestOptions?: NativeCompactionRequestOptions): Promise<CompactionResult>;
 //# sourceMappingURL=compaction.d.ts.map
\ No newline at end of file
diff --git a/dist/core/compaction/compaction.js b/dist/core/compaction/compaction.js
index 202e45d4d595a50b10d639c32dcc685ef5b34d69..9df5aff99396c286453d06cb8e62c6f6605d54f7 100644
--- a/dist/core/compaction/compaction.js
+++ b/dist/core/compaction/compaction.js
@@ -4,10 +4,11 @@
  * Pure functions for compaction logic. The session manager handles I/O,
  * and after compaction the session is reloaded.
  */
-import { contentText, getCurrentSystemMessage, normalizeContext, retryAssistantCall, uuidv7, } from "@earendil-works/pi-ai";
+import { calculateCost, contentText, getCurrentSystemMessage, normalizeContext, retryAssistantCall, uuidv7 } from "@earendil-works/pi-ai";
+import { compactResponses } from "@earendil-works/pi-ai/api/openai-responses";
 import { completeSimple } from "@earendil-works/pi-ai/compat";
 import { convertToLlm } from "../messages.js";
-import { buildSessionProjection, sessionEntryToContextMessages, } from "../session-manager.js";
+import { buildSessionProjection, estimateReconstructionMessageTokens, getNativeCredentialFingerprint, nativeCredentialFingerprint, sessionEntryToContextMessages } from "../session-manager.js";
 import { computeFileLists, createFileOps, extractFileOpsFromMessage, formatFileOperations, SUMMARIZATION_SYSTEM_PROMPT, serializeConversation, } from "./utils.js";
 /**
  * Extract file operations from messages and previous compaction entries.
@@ -75,6 +76,7 @@ export const DEFAULT_COMPACTION_SETTINGS = {
     enabled: true,
     reserveTokens: 16384,
     keepRecentTokens: 20000,
+    thresholdPercent: 80,
 };
 // ============================================================================
 // Token calculation
@@ -194,7 +196,53 @@ export function estimateProjectedContextTokens(projection, branchEntries) {
 export function shouldCompact(contextTokens, contextWindow, settings) {
     if (!settings.enabled)
         return false;
-    return contextTokens > contextWindow - settings.reserveTokens;
+    const configuredThreshold = contextWindow * (settings.thresholdPercent / 100);
+    const reserveThreshold = Math.max(0, contextWindow - settings.reserveTokens);
+    return contextTokens >= Math.min(configuredThreshold, reserveThreshold);
+}
+export function selectCompactionMechanism(model) {
+    const responsesApi = model.api === "openai-responses" ||
+        model.api === "openai-codex-responses";
+    return responsesApi && model.compat?.supportsCompaction === true ? "native" : "portable";
+}
+function nativeCompactionIdentity(model, apiKey, headers) {
+    return {
+        api: model.api,
+        provider: model.provider,
+        baseUrl: model.baseUrl,
+        compactionBaseUrl: model.compat?.compactionBaseUrl ?? model.baseUrl,
+        modelId: model.id,
+        credentialFingerprint: nativeCredentialFingerprint(apiKey, headers),
+    };
+}
+function isCompatibleNativeDetails(details, model) {
+    if (details?.schemaVersion !== 1 || details?.mechanism !== "native")
+        return true;
+    if (!model)
+        return true;
+    if (selectCompactionMechanism(model) !== "native")
+        return false;
+    const identity = details.identity;
+    return identity?.api === model.api &&
+        identity?.provider === model.provider &&
+        identity?.baseUrl === model.baseUrl &&
+        identity?.compactionBaseUrl === (model.compat?.compactionBaseUrl ?? model.baseUrl) &&
+        identity?.modelId === model.id &&
+        identity?.credentialFingerprint === getNativeCredentialFingerprint(model);
+}
+function nativeCompactionUsage(usage, model) {
+    const cached = usage.input_tokens_details?.cached_tokens ?? 0;
+    const normalized = {
+        input: Math.max(0, usage.input_tokens - cached),
+        output: usage.output_tokens,
+        cacheRead: cached,
+        cacheWrite: 0,
+        reasoning: usage.output_tokens_details?.reasoning_tokens ?? 0,
+        totalTokens: usage.total_tokens,
+        cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0, total: 0 },
+    };
+    normalized.cost = calculateCost(model, normalized);
+    return normalized;
 }
 // ============================================================================
 // Cut point detection
@@ -397,76 +445,21 @@ export function findCutPoint(entries, startIndex, endIndex, keepRecentTokens) {
 // ============================================================================
 // Summarization
 // ============================================================================
-const SUMMARIZATION_PROMPT = `The messages above are a conversation to summarize. Create a structured context checkpoint summary that another LLM will use to continue the work.
-
-Use this EXACT format:
-
-## Goal
-[What is the user trying to accomplish? Can be multiple items if the session covers different tasks.]
-
-## Constraints & Preferences
-- [Any constraints, preferences, or requirements mentioned by user]
-- [Or "(none)" if none were mentioned]
-
-## Progress
-### Done
-- [x] [Completed tasks/changes]
-
-### In Progress
-- [ ] [Current work]
-
-### Blocked
-- [Issues preventing progress, if any]
-
-## Key Decisions
-- **[Decision]**: [Brief rationale]
-
-## Next Steps
-1. [Ordered list of what should happen next]
-
-## Critical Context
-- [Any data, examples, or references needed to continue]
-- [Or "(none)" if not applicable]
-
-Keep each section concise. Preserve exact file paths, function names, and error messages.`;
-const UPDATE_SUMMARIZATION_INSTRUCTIONS = `Update the existing structured summary with new information. RULES:
-- PRESERVE all existing information from the previous summary
-- ADD new progress, decisions, and context from the new messages
-- UPDATE the Progress section: move items from "In Progress" to "Done" when completed
-- UPDATE "Next Steps" based on what was accomplished
-- PRESERVE exact file paths, function names, and error messages
-- If something is no longer relevant, you may remove it
-
-Use this EXACT format:
-
-## Goal
-[Preserve existing goals, add new ones if the task expanded]
-
-## Constraints & Preferences
-- [Preserve existing, add new ones discovered]
-
-## Progress
-### Done
-- [x] [Include previously done items AND newly completed items]
-
-### In Progress
-- [ ] [Current work - update based on progress]
-
-### Blocked
-- [Current blockers - remove if resolved]
-
-## Key Decisions
-- **[Decision]**: [Brief rationale] (preserve all previous, add new)
-
-## Next Steps
-1. [Update based on current state]
-
-## Critical Context
-- [Preserve important context, add new if needed]
-
-Keep each section concise. Preserve exact file paths, function names, and error messages.`;
+const SUMMARIZATION_PROMPT = `You are performing a CONTEXT CHECKPOINT COMPACTION. Create a handoff summary for another LLM that will resume the task.
+Include:
+- Current progress and key decisions made
+- Important context, constraints, or user preferences
+- What remains to be done (clear next steps)
+- Any critical data, examples, or references needed to continue
+Be concise, structured, and focused on helping the next LLM seamlessly continue the work.`;
+const UPDATE_SUMMARIZATION_INSTRUCTIONS = `Update the existing handoff summary with the new conversation messages while preserving the same four-part contract.
+Include:
+- Current progress and key decisions made
+- Important context, constraints, or user preferences
+- What remains to be done (clear next steps)
+- Any critical data, examples, or references needed to continue
+Preserve still-relevant details from the previous summary, including exact file paths, function names, and error messages. Remove only information that is no longer relevant. Be concise, structured, and focused on helping the next LLM seamlessly continue the work.`;
 const UPDATE_SUMMARIZATION_PROMPT = `The messages above are NEW conversation messages to incorporate into the existing summary provided in <previous-summary> tags.
-
 ${UPDATE_SUMMARIZATION_INSTRUCTIONS}`;
 /**
  * Returns an error message when a summarization response cannot safely be persisted.
@@ -528,24 +521,22 @@ function buildSummarizationContext(promptText) {
         ],
     });
 }
-/** Generate or update a conversation summary and return its provider usage. */
-export async function generateSummaryWithUsage(currentMessages, model, reserveTokens, apiKey, headers, signal, customInstructions, previousSummary, thinkingLevel, streamFn, env, retry, callbacks, sessionId) {
-    const maxTokens = Math.min(Math.floor(0.8 * reserveTokens), model.maxTokens > 0 ? model.maxTokens : Number.POSITIVE_INFINITY);
-    // Use update prompt if we have a previous summary, otherwise initial prompt
+function buildSummaryPrompt(currentMessages, customInstructions, previousSummary) {
     let basePrompt = previousSummary ? UPDATE_SUMMARIZATION_PROMPT : SUMMARIZATION_PROMPT;
     if (customInstructions) {
         basePrompt = `${basePrompt}\n\nAdditional focus: ${customInstructions}`;
     }
-    // Serialize conversation to text so model doesn't try to continue it
-    // Convert to LLM messages first (handles custom types like bashExecution, custom, etc.)
-    const llmMessages = convertToLlm(currentMessages);
-    const conversationText = serializeConversation(llmMessages);
-    // Build the prompt with conversation wrapped in tags
+    const conversationText = serializeConversation(convertToLlm(currentMessages));
     let promptText = `<conversation>\n${conversationText}\n</conversation>\n\n`;
     if (previousSummary) {
         promptText += `<previous-summary>\n${previousSummary}\n</previous-summary>\n\n`;
     }
-    promptText += basePrompt;
+    return promptText + basePrompt;
+}
+/** Generate or update a conversation summary and return its provider usage. */
+export async function generateSummaryWithUsage(currentMessages, model, reserveTokens, apiKey, headers, signal, customInstructions, previousSummary, thinkingLevel, streamFn, env, retry, callbacks, sessionId) {
+    const maxTokens = Math.min(Math.floor(0.8 * reserveTokens), model.maxTokens > 0 ? model.maxTokens : Number.POSITIVE_INFINITY);
+    const promptText = buildSummaryPrompt(currentMessages, customInstructions, previousSummary);
     const completionOptions = createSummarizationOptions(model, maxTokens, apiKey, headers, env, signal, thinkingLevel, sessionId);
     const response = await completeSummarization(model, buildSummarizationContext(promptText), completionOptions, streamFn, retry, callbacks);
     const failure = getSummarizationFailure(response, "Summarization");
@@ -577,9 +568,8 @@ function findProjectedCutPoint(entries, startIndex, endIndex, keepRecentTokens)
         if (entry.sourceEntry.type !== "compaction" && entry.messages.some(isCutPointMessage))
             cutPoints.push(i);
     }
-    if (cutPoints.length === 0) {
+    if (cutPoints.length === 0)
         return { firstKeptEntryIndex: startIndex, turnStartIndex: -1, isSplitTurn: false };
-    }
     let accumulatedTokens = 0;
     let exceededBudget = false;
     let cutIndex = cutPoints[0];
@@ -594,9 +584,6 @@ function findProjectedCutPoint(entries, startIndex, endIndex, keepRecentTokens)
             break;
         }
     }
-    // A recovery attempt and its omission edits are context-invisible after the last
-    // visible input. Advance only for a closed suffix containing an omitted assistant
-    // attempt; arbitrary metadata must not move the cut past unsent input.
     const suffix = entries.slice(cutIndex + 1, endIndex);
     const isIntrinsicallyVisible = (entry) => entry.sourceEntry.type !== "context_edit" && sessionEntryToContextMessages(entry.sourceEntry).length > 0;
     const isOmitted = (entry) => isIntrinsicallyVisible(entry) && entry.messages.length === 0;
@@ -618,58 +605,62 @@ function findProjectedCutPoint(entries, startIndex, endIndex, keepRecentTokens)
     }
     const startsTurn = isProjectedTurnStart(entries[cutIndex]);
     const turnStartIndex = startsTurn ? -1 : findProjectedTurnStartIndex(entries, cutIndex, startIndex);
-    return {
-        firstKeptEntryIndex: cutIndex,
-        turnStartIndex,
-        isSplitTurn: !startsTurn && turnStartIndex !== -1,
-    };
+    return { firstKeptEntryIndex: cutIndex, turnStartIndex, isSplitTurn: !startsTurn && turnStartIndex !== -1 };
 }
-export function prepareCompaction(pathEntries, settings) {
-    if (pathEntries.length > 0 && pathEntries[pathEntries.length - 1].type === "compaction") {
+function prepareCompactionProjection(pathEntries, settings, model, allowNativeProjection, requestOptions) {
+    if (pathEntries.length > 0 && pathEntries[pathEntries.length - 1].type === "compaction")
         return undefined;
+    const projectionModel = !allowNativeProjection && model
+        ? { ...model, compat: { ...(model.compat ?? {}), supportsCompaction: false } }
+        : model;
+    const nativeCompaction = allowNativeProjection && model && selectCompactionMechanism(model) === "native";
+    let requestOverheadTokens = nativeCompaction
+        ? requestOptions.nativeRequestOverheadTokens
+        : estimatePortableCompactionRequestOverheadTokens(requestOptions.portableCustomInstructions);
+    let projection = buildSessionProjection(pathEntries, undefined, undefined, projectionModel, { requestOverheadTokens });
+    let previousSummary = projection.entries.find((entry) => entry.sourceEntry.type === "compaction" && entry.messages.length > 0)?.sourceEntry.summary;
+    if (!nativeCompaction) {
+        const maxPortableRefits = projection.messages.length + 1;
+        for (let attempt = 0; attempt < maxPortableRefits; attempt++) {
+            const serializedOverhead = estimatePortableCompactionRequestOverheadTokens(requestOptions.portableCustomInstructions, previousSummary, projection.messages.filter((message) => message.role !== "system"));
+            if (serializedOverhead <= requestOverheadTokens)
+                break;
+            requestOverheadTokens = serializedOverhead;
+            projection = buildSessionProjection(pathEntries, undefined, undefined, projectionModel, { requestOverheadTokens });
+            previousSummary = projection.entries.find((entry) => entry.sourceEntry.type === "compaction" && entry.messages.length > 0)?.sourceEntry.summary;
+        }
     }
-    const projection = buildSessionProjection(pathEntries);
     const projectedEntries = projection.entries;
     const sourceEntries = projectedEntries.map((entry) => entry.sourceEntry);
-    // The newest compaction is projected first. Older compaction entries can still
-    // occur in its retained raw range, but their projected contribution is empty.
     const prevCompactionIndex = projectedEntries.findIndex((entry) => entry.sourceEntry.type === "compaction" && entry.messages.length > 0);
-    let previousSummary;
     let boundaryStart = 0;
     if (prevCompactionIndex >= 0) {
         previousSummary = projectedEntries[prevCompactionIndex].sourceEntry.summary;
-        // The canonical projection has already selected the previous compaction's retained tail.
         boundaryStart = prevCompactionIndex + 1;
     }
     const boundaryEnd = projectedEntries.length;
     const tokensBefore = estimateProjectedContextTokens(projection, pathEntries).tokens;
-    const cutPoint = findProjectedCutPoint(projectedEntries, boundaryStart, boundaryEnd, settings.keepRecentTokens);
+    const hardTailAllowance = model && Number.isFinite(model.contextWindow) && model.contextWindow > 0
+        ? Math.max(0, model.contextWindow - Math.min(settings.reserveTokens, model.contextWindow))
+        : settings.keepRecentTokens;
+    const cutPoint = findProjectedCutPoint(projectedEntries, boundaryStart, boundaryEnd, Math.min(settings.keepRecentTokens, hardTailAllowance));
     const firstKeptEntry = projectedEntries[cutPoint.firstKeptEntryIndex]?.sourceEntry;
     if (!firstKeptEntry?.id)
         return undefined;
-    const firstKeptEntryId = firstKeptEntry.id;
     const historyEnd = cutPoint.isSplitTurn ? cutPoint.turnStartIndex : cutPoint.firstKeptEntryIndex;
-    const messagesToSummarize = projectedEntries
-        .slice(boundaryStart, historyEnd)
-        .flatMap(getMessagesFromProjectedEntryForCompaction);
+    const messagesToSummarize = projectedEntries.slice(boundaryStart, historyEnd).flatMap(getMessagesFromProjectedEntryForCompaction);
     const turnPrefixMessages = cutPoint.isSplitTurn
-        ? projectedEntries
-            .slice(cutPoint.turnStartIndex, cutPoint.firstKeptEntryIndex)
-            .flatMap(getMessagesFromProjectedEntryForCompaction)
+        ? projectedEntries.slice(cutPoint.turnStartIndex, cutPoint.firstKeptEntryIndex).flatMap(getMessagesFromProjectedEntryForCompaction)
         : [];
-    if (messagesToSummarize.length === 0 && turnPrefixMessages.length === 0)
+    if (messagesToSummarize.length === 0 && turnPrefixMessages.length === 0 && !nativeCompaction)
         return undefined;
-    // Extract file operations from edited model-visible messages and the previous compaction.
     const fileOps = extractFileOperations(messagesToSummarize, sourceEntries, prevCompactionIndex);
-    // Also extract file ops from turn prefix if splitting
-    if (cutPoint.isSplitTurn) {
-        for (const msg of turnPrefixMessages) {
-            extractFileOpsFromMessage(msg, fileOps);
-        }
-    }
+    for (const message of turnPrefixMessages)
+        extractFileOpsFromMessage(message, fileOps);
     return {
-        firstKeptEntryId,
+        firstKeptEntryId: firstKeptEntry.id,
         messagesToSummarize,
+        messagesForNativeCompaction: projection.messages,
         turnPrefixMessages,
         isSplitTurn: cutPoint.isSplitTurn,
         tokensBefore,
@@ -678,6 +669,26 @@ export function prepareCompaction(pathEntries, settings) {
         settings,
     };
 }
+export function estimatePortableCompactionRequestOverheadTokens(customInstructions, previousSummary, messages = []) {
+    const summaryPrompt = buildSummaryPrompt(messages, customInstructions, previousSummary);
+    const conversationText = serializeConversation(convertToLlm(messages));
+    const turnPrefixPrompt = `<conversation>
+${conversationText}
+</conversation>
+
+${TURN_PREFIX_SUMMARIZATION_PROMPT}`;
+    const estimateTextTokens = (text) => Math.ceil(Math.max(text.length, new TextEncoder().encode(text).length) / 4);
+    const serializedRequestTokens = estimateTextTokens(SUMMARIZATION_SYSTEM_PROMPT) +
+        Math.max(estimateTextTokens(summaryPrompt), estimateTextTokens(turnPrefixPrompt));
+    const reconstructionTokens = messages.reduce((total, message) => total + estimateReconstructionMessageTokens(message), 0);
+    return Math.max(0, serializedRequestTokens - reconstructionTokens);
+}
+export function prepareCompaction(pathEntries, settings, model, requestOptions = {}) {
+    const preparation = prepareCompactionProjection(pathEntries, settings, model, true, requestOptions);
+    if (!preparation || !model || selectCompactionMechanism(model) !== "native")
+        return preparation;
+    return { ...preparation, portableFallback: prepareCompactionProjection(pathEntries, settings, model, false, requestOptions) };
+}
 // ============================================================================
 // Main compaction function
 // ============================================================================
@@ -694,7 +705,110 @@ Create a concise checkpoint of the user's request and the progress shown above.
 ## Context Needed to Continue
 - [Information from these messages needed to understand the later work]

-Only summarize information explicitly present above. Do not infer or recreate later messages.`;
+Only summarize information explicitly present above. Do not infer or recreate later messages. Be concise. Focus on what's needed to understand the kept suffix.`;
+const NATIVE_REPLACEMENT_IMAGE_DETAILS = new Set(["low", "high", "auto", "original"]);
+const NATIVE_REPLACEMENT_FILE_DETAILS = new Set(["low", "high"]);
+const NATIVE_REPLACEMENT_STATUSES = new Set(["in_progress", "completed", "incomplete"]);
+function isRecord(value) {
+    return value !== null && typeof value === "object" && !Array.isArray(value);
+}
+function hasOptionalString(record, key, nullable = false) {
+    return (record[key] === undefined ||
+        (nullable && record[key] === null) ||
+        typeof record[key] === "string");
+}
+function isNativeReplacementContent(content) {
+    if (!isRecord(content) || typeof content.type !== "string")
+        return false;
+    switch (content.type) {
+        case "input_text":
+            return typeof content.text === "string";
+        case "input_image":
+            return (NATIVE_REPLACEMENT_IMAGE_DETAILS.has(content.detail) &&
+                hasOptionalString(content, "file_id", true) &&
+                hasOptionalString(content, "image_url", true) &&
+                (typeof content.file_id === "string" || typeof content.image_url === "string"));
+        case "input_file":
+            return ((content.detail === undefined || NATIVE_REPLACEMENT_FILE_DETAILS.has(content.detail)) &&
+                hasOptionalString(content, "file_data") &&
+                hasOptionalString(content, "file_id", true) &&
+                hasOptionalString(content, "file_url") &&
+                hasOptionalString(content, "filename") &&
+                (typeof content.file_data === "string" ||
+                    typeof content.file_id === "string" ||
+                    typeof content.file_url === "string"));
+        default:
+            return false;
+    }
+}
+function isNativeReplacementMessage(item) {
+    return (item.type === "message" &&
+        typeof item.id === "string" &&
+        item.role === "user" &&
+        (item.status === undefined || NATIVE_REPLACEMENT_STATUSES.has(item.status)) &&
+        Array.isArray(item.content) &&
+        item.content.length > 0 &&
+        item.content.every(isNativeReplacementContent));
+}
+function isNativeCompactionItem(item) {
+    return (item.type === "compaction" &&
+        typeof item.id === "string" &&
+        typeof item.encrypted_content === "string" &&
+        hasOptionalString(item, "created_by"));
+}
+function toNativeCompactionRequestItem(item) {
+    return {
+        type: "compaction",
+        id: item.id,
+        encrypted_content: item.encrypted_content,
+    };
+}
+function toNativeReplacementRequestContent(content) {
+    switch (content.type) {
+        case "input_text":
+            return { type: "input_text", text: content.text };
+        case "input_image":
+            return {
+                type: "input_image",
+                detail: content.detail,
+                ...(content.file_id !== undefined ? { file_id: content.file_id } : {}),
+                ...(content.image_url !== undefined ? { image_url: content.image_url } : {}),
+            };
+        case "input_file":
+            return {
+                type: "input_file",
+                ...(content.detail !== undefined ? { detail: content.detail } : {}),
+                ...(content.file_data !== undefined ? { file_data: content.file_data } : {}),
+                ...(content.file_id !== undefined ? { file_id: content.file_id } : {}),
+                ...(content.file_url !== undefined ? { file_url: content.file_url } : {}),
+                ...(content.filename !== undefined ? { filename: content.filename } : {}),
+            };
+        default:
+            throw new Error("Native compaction response contained unsupported retained content");
+    }
+}
+function toNativeReplacementRequestMessage(item) {
+    return {
+        type: "message",
+        role: "user",
+        ...(item.status !== undefined ? { status: item.status } : {}),
+        content: item.content.map(toNativeReplacementRequestContent),
+    };
+}
+function toNativeReplacementRequestItem(item) {
+    return isNativeCompactionItem(item)
+        ? toNativeCompactionRequestItem(item)
+        : toNativeReplacementRequestMessage(item);
+}
+function isNativeReplacementItem(item) {
+    return isRecord(item) && (isNativeCompactionItem(item) || isNativeReplacementMessage(item));
+}
+const NATIVE_ENDPOINT_UNAVAILABLE_STATUSES = new Set([404, 405, 501]);
+function isNativeEndpointUnavailable(error) {
+    return (isRecord(error) &&
+        typeof error.status === "number" &&
+        NATIVE_ENDPOINT_UNAVAILABLE_STATUSES.has(error.status));
+}
 /**
  * Generate summaries for compaction using prepared data.
  * Returns CompactionResult - SessionManager adds uuid/parentUuid when saving.
@@ -703,8 +817,72 @@ Only summarize information explicitly present above. Do not infer or recreate la
  * @param customInstructions - Optional custom focus for the summary
  * @param sessionId - Optional routing session ID forwarded without enabling prompt caching
  */
-export async function compact(preparation, model, apiKey, headers, customInstructions, signal, thinkingLevel, streamFn, env, retry, callbacks, sessionId) {
-    const { firstKeptEntryId, messagesToSummarize, turnPrefixMessages, isSplitTurn, tokensBefore, previousSummary, fileOps, settings, } = preparation;
+export async function compact(preparation, model, apiKey, headers, customInstructions, signal, thinkingLevel, streamFn, env, retry, callbacks, sessionId, systemPrompt = "", transformPreparation, nativeRequestOptions) {
+    let selectedPreparation = preparation;
+    if (selectCompactionMechanism(model) === "native") {
+        const nativePreparation = transformPreparation
+            ? await transformPreparation(preparation, "native")
+            : preparation;
+        try {
+            const nativeMessages = nativeRequestOptions?.convertMessages
+                ? await nativeRequestOptions.convertMessages(nativePreparation.messagesForNativeCompaction)
+                : convertToLlm(nativePreparation.messagesForNativeCompaction);
+            const existingSystem = getCurrentSystemMessage(nativeMessages);
+            const nativeInstructions = [
+                existingSystem ? undefined : systemPrompt,
+                customInstructions?.trim()
+                    ? `Additional compaction instructions:\n${customInstructions.trim()}`
+                    : undefined,
+            ].filter(Boolean).join("\n\n");
+            const response = await compactResponses(model, normalizeContext({
+                systemPrompt: nativeInstructions || undefined,
+                messages: nativeMessages,
+            }), {
+                onPayload: nativeRequestOptions?.onPayload, onResponse: nativeRequestOptions?.onResponse, timeoutMs: nativeRequestOptions?.timeoutMs,
+                apiKey,
+                headers: nativeRequestOptions?.headers ?? headers,
+                signal,
+                env,
+                sessionId,
+                retryPolicy: retry,
+                retryCallbacks: callbacks,
+            });
+            if (!Array.isArray(response.output) ||
+                !response.output.some((item) => isRecord(item) && isNativeCompactionItem(item))) {
+                throw new Error("Native compaction response did not contain a valid compaction item");
+            }
+            const compactionItemCount = response.output.filter((item) => isRecord(item) && isNativeCompactionItem(item)).length;
+            if (!response.output.every(isNativeReplacementItem) ||
+                compactionItemCount !== 1 ||
+                !isNativeCompactionItem(response.output.at(-1))) {
+                throw new Error("Native compaction response did not contain only valid replacement items");
+            }
+            return {
+                summary: "Native compaction checkpoint",
+                firstKeptEntryId: "native-replacement",
+                tokensBefore: preparation.tokensBefore,
+                usage: nativeCompactionUsage(response.usage, model),
+                details: {
+                    schemaVersion: 1,
+                    mechanism: "native",
+                    identity: nativeCompactionIdentity(model, apiKey, nativeRequestOptions?.headers ?? headers),
+                    items: response.output.map(toNativeReplacementRequestItem),
+                },
+            };
+        }
+        catch (error) {
+            if (signal?.aborted || !isNativeEndpointUnavailable(error) || !preparation.portableFallback) {
+                throw error;
+            }
+            selectedPreparation = transformPreparation
+                ? await transformPreparation(preparation.portableFallback, "portable")
+                : preparation.portableFallback;
+        }
+    }
+    else if (transformPreparation) {
+        selectedPreparation = await transformPreparation(preparation, "portable");
+    }
+    const { firstKeptEntryId, messagesToSummarize, turnPrefixMessages, isSplitTurn, tokensBefore, previousSummary, fileOps, settings, } = selectedPreparation;
     // Generate summaries and merge into one
     let summary;
     let summaryUsage;
@@ -738,7 +916,13 @@ export async function compact(preparation, model, apiKey, headers, customInstruc
         firstKeptEntryId,
         tokensBefore,
         usage: summaryUsage,
-        details: { readFiles, modifiedFiles },
+        details: {
+            schemaVersion: 1,
+            mechanism: "portable",
+            contract: "codex-handoff-v1",
+            readFiles,
+            modifiedFiles,
+        },
     };
 }
 /**
diff --git a/dist/core/messages.d.ts b/dist/core/messages.d.ts
index 9cdda75154578d3460d6507715ae59b2c7704873..2f06a8ba569081ca949dd695cd3ba6b0b051c312 100644
--- a/dist/core/messages.d.ts
+++ b/dist/core/messages.d.ts
@@ -47,6 +47,8 @@ export interface CompactionSummaryMessage {
     role: "compactionSummary";
     summary: string;
     tokensBefore: number;
+    details?: unknown;
+    reason?: "manual" | "threshold" | "overflow";
     timestamp: number;
 }
 declare module "@earendil-works/pi-agent-core" {
@@ -62,7 +64,7 @@ declare module "@earendil-works/pi-agent-core" {
  */
 export declare function bashExecutionToText(msg: BashExecutionMessage): string;
 export declare function createBranchSummaryMessage(summary: string, fromId: string, timestamp: string): BranchSummaryMessage;
-export declare function createCompactionSummaryMessage(summary: string, tokensBefore: number, timestamp: string): CompactionSummaryMessage;
+export declare function createCompactionSummaryMessage(summary: string, tokensBefore: number, timestamp: string, details?: unknown, reason?: "manual" | "threshold" | "overflow"): CompactionSummaryMessage;
 /** Convert CustomMessageEntry to AgentMessage format */
 export declare function createCustomMessage(customType: string, content: string | (TextContent | ImageContent)[], display: boolean, details: unknown | undefined, timestamp: string): CustomMessage;
 /**
diff --git a/dist/core/messages.js b/dist/core/messages.js
index bcaeaae631aa9992e07cfc43baca2edbe4d6ab30..c136d2007445beb13113807b903c7682f1b04fb6 100644
--- a/dist/core/messages.js
+++ b/dist/core/messages.js
@@ -45,11 +45,13 @@ export function createBranchSummaryMessage(summary, fromId, timestamp) {
         timestamp: new Date(timestamp).getTime(),
     };
 }
-export function createCompactionSummaryMessage(summary, tokensBefore, timestamp) {
+export function createCompactionSummaryMessage(summary, tokensBefore, timestamp, details, reason) {
     return {
         role: "compactionSummary",
         summary: summary,
         tokensBefore,
+        details,
+        reason,
         timestamp: new Date(timestamp).getTime(),
     };
 }
@@ -101,6 +103,31 @@ export function convertToLlm(messages) {
                     timestamp: m.timestamp,
                 };
             case "compactionSummary":
+                if (m.details?.schemaVersion === 1 && m.details?.mechanism === "native" &&
+                    Array.isArray(m.details.items)) {
+                    const identity = m.details.identity;
+                    return {
+                        role: "assistant",
+                        content: m.details.items.map((item) => ({
+                            type: "thinking",
+                            thinking: "",
+                            thinkingSignature: JSON.stringify(item),
+                        })),
+                        api: identity.api,
+                        provider: identity.provider,
+                        model: identity.modelId,
+                        usage: {
+                            input: 0,
+                            output: 0,
+                            cacheRead: 0,
+                            cacheWrite: 0,
+                            totalTokens: 0,
+                            cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0, total: 0 },
+                        },
+                        stopReason: "stop",
+                        timestamp: m.timestamp,
+                    };
+                }
                 return {
                     role: "user",
                     content: [
diff --git a/dist/core/model-runtime.d.ts b/dist/core/model-runtime.d.ts
index e6fbba53dad4459e7bde9294e3bd103b8cc7572e..3fa56f12c17101d837abe0902f2bd51de77b5d6e 100644
--- a/dist/core/model-runtime.d.ts
+++ b/dist/core/model-runtime.d.ts
@@ -49,6 +49,7 @@ export declare class ModelRuntime implements Models {
     private readonly providerAvailabilitySeq;
     private availabilityError;
     private readonly credentialOperations;
+    private readonly preparedRequestAuth;
     private constructor();
     static create(options?: CreateModelRuntimeOptions): Promise<ModelRuntime>;
     private configureRadiusProviders;
@@ -83,6 +84,10 @@ export declare class ModelRuntime implements Models {
     removeRuntimeApiKey(providerId: string, options?: AuthOperationOptions): Promise<void>;
     listCredentials(options?: AuthOperationOptions): Promise<readonly CredentialInfo[]>;
     getProviderAuthStatus(providerId: string): AuthStatus;
+    prepareModelRequest<TApi extends Api>(model: Model<TApi>, overrides?: ModelRuntimeAuthOverrides): Promise<{
+        model: Model<TApi>;
+        resolution: AuthResult;
+    }>;
     private prepareRequest;
     stream<TApi extends Api>(model: Model<TApi>, context: Context, options?: ModelsApiStreamOptions<TApi>): AssistantMessageEventStream;
     complete<TApi extends Api>(model: Model<TApi>, context: Context, options?: ModelsApiStreamOptions<TApi>): Promise<AssistantMessage>;
diff --git a/dist/core/model-runtime.js b/dist/core/model-runtime.js
index 81bf752b39ee55ff08015bca5e78770bf74409fa..16a87b433e3d614bc9ecddadfa18c41410eb8318 100644
--- a/dist/core/model-runtime.js
+++ b/dist/core/model-runtime.js
@@ -60,6 +60,7 @@ export class ModelRuntime {
     providerAvailabilitySeq = new Map();
     availabilityError;
     credentialOperations = new Map();
+    preparedRequestAuth = new WeakMap();
     constructor(credentials, config, modelsPath, modelsStore, providers, modelNetworkEnabled) {
         this.credentials = credentials;
         this.config = config;
@@ -419,15 +420,30 @@ export class ModelRuntime {
         const check = this.snapshot.auth.get(providerId);
         return check ? { configured: true, source: "environment", label: check.source } : { configured: false };
     }
+    async prepareModelRequest(model, overrides = {}) {
+        const resolution = await this.getAuth(model, overrides);
+        if (!resolution)
+            throw new ModelsError("auth", `Provider is not configured: ${model.provider}`);
+        const requestModel = {
+            ...model,
+            baseUrl: resolution.auth.baseUrl || model.baseUrl,
+        };
+        this.preparedRequestAuth.set(requestModel, resolution);
+        return { model: requestModel, resolution };
+    }
     async prepareRequest(model, options) {
         const provider = this.models.getProvider(model.provider);
         if (!provider)
             throw new ModelsError("provider", `Unknown provider: ${model.provider}`);
-        const resolution = await this.getAuth(model, {
-            apiKey: options?.apiKey,
-            env: options?.env,
-            signal: options?.signal,
-        });
+        const preparedResolution = this.preparedRequestAuth.get(model);
+        if (preparedResolution)
+            this.preparedRequestAuth.delete(model);
+        const resolution = preparedResolution ??
+            (await this.getAuth(model, {
+                apiKey: options?.apiKey,
+                env: options?.env,
+                signal: options?.signal,
+            }));
         if (!resolution)
             throw new ModelsError("auth", `Provider is not configured: ${model.provider}`);
         const { transformHeaders, ...rawProviderOptions } = options ?? {};
diff --git a/dist/core/sdk.js b/dist/core/sdk.js
index cb6d649d7db18dca9b86c1248d232b54298f3ffb..ee6d9578ac39d68de83832c72b85442fbff30f14 100644
--- a/dist/core/sdk.js
+++ b/dist/core/sdk.js
@@ -12,7 +12,7 @@ import { findInitialModel } from "./model-resolver.js";
 import { ModelRuntime } from "./model-runtime.js";
 import { mergeProviderAttributionHeaders } from "./provider-attribution.js";
 import { DefaultResourceLoader } from "./resource-loader.js";
-import { getDefaultSessionDir, SessionManager } from "./session-manager.js";
+import { buildSessionContext, getDefaultSessionDir, SessionManager, takePreparedProviderHeaders, withNativeCredentialFingerprint, } from "./session-manager.js";
 import { SettingsManager } from "./settings-manager.js";
 import { time } from "./timings.js";
 import { createBashTool, createCodingTools, createEditTool, createFindTool, createGrepTool, createLsTool, createPowerShellTool, createReadOnlyTools, createReadTool, createWriteTool, withFileMutationQueue, } from "./tools/index.js";
@@ -79,9 +79,11 @@ export async function createAgentSession(options = {}) {
         time("resourceLoader.reload");
     }
     // Check if session has existing data to restore
-    const existingSession = sessionManager.buildSessionContext();
-    const hasExistingSession = existingSession.messages.length > 0;
-    const hasThinkingEntry = sessionManager.getBranch().some((entry) => entry.type === "thinking_level_change");
+    const branchEntries = sessionManager.getBranch();
+    const projectSession = (targetModel, forceNativeReconstruction = false) => buildSessionContext(branchEntries, sessionManager.getLeafId(), undefined, targetModel, { forceNativeReconstruction });
+    const existingSession = projectSession(options.model);
+    const hasExistingSession = branchEntries.length > 0;
+    const hasThinkingEntry = branchEntries.some((entry) => entry.type === "thinking_level_change");
     let model = options.model;
     let modelFallbackMessage;
     // If session has data, try to restore model from it
@@ -137,6 +139,40 @@ export async function createAgentSession(options = {}) {
     else {
         thinkingLevel = clampThinkingLevel(model, thinkingLevel);
     }
+    const selectedModel = model;
+    const latestCompaction = [...sessionManager.getBranch()]
+        .reverse()
+        .find((entry) => entry.type === "compaction");
+    const hasNativeCheckpoint = latestCompaction?.details?.schemaVersion === 1 &&
+        latestCompaction.details.mechanism === "native";
+    let forceNativeReconstruction = false;
+    // Credential resolution may select a provider-specific endpoint (for
+    // example, a subscription backend). Resolve it only when an identity-bound
+    // native checkpoint is active. If credentials cannot be refreshed, restore
+    // raw history so read-only session inspection remains available offline.
+    if (model && hasNativeCheckpoint) {
+        try {
+            const auth = await modelRuntime.getAuth(model);
+            if (auth?.auth.baseUrl) {
+                model = { ...model, baseUrl: auth.auth.baseUrl };
+            }
+            if (auth) {
+                model = withNativeCredentialFingerprint({ ...model }, auth.auth.apiKey, auth.auth.headers);
+            }
+            else {
+                forceNativeReconstruction = true;
+            }
+        }
+        catch {
+            forceNativeReconstruction = true;
+        }
+    }
+    // Rebuild model-visible history only after the final target model is known.
+    // Native compaction checkpoints are identity-bound; a restored fallback model
+    // must receive append-only raw reconstruction instead of the source checkpoint.
+    const resolvedExistingSession = model
+        ? projectSession(model, forceNativeReconstruction)
+        : existingSession;
     const defaultActiveToolNames = ["read", "bash", "edit", "write"];
     const configuredDefaultToolNames = settingsManager.getDefaultTools();
     const allowedToolNames = options.tools ?? (options.noTools === "all" ? [] : undefined);
@@ -187,6 +223,9 @@ export async function createAgentSession(options = {}) {
             maxRetries: options.maxRetries ?? providerRetrySettings.maxRetries,
             maxRetryDelayMs: options.maxRetryDelayMs ?? providerRetrySettings.maxRetryDelayMs,
             transformHeaders: async (requestHeaders) => {
+                const preparedHeaders = takePreparedProviderHeaders(requestModel);
+                if (preparedHeaders)
+                    return preparedHeaders;
                 const headers = mergeProviderAttributionHeaders(requestModel, settingsManager, options.sessionId, requestHeaders);
                 return headerRunner?.hasHandlers("before_provider_headers")
                     ? headerRunner.emitBeforeProviderHeaders(headers ?? {})
@@ -227,7 +266,7 @@ export async function createAgentSession(options = {}) {
             model,
             thinkingLevel,
             tools: [],
-            messages: existingSession.messages,
+            messages: resolvedExistingSession.messages,
         },
         convertToLlm: convertToLlmWithBlockImages,
         streamFn: async (model, context, options) => {
@@ -272,6 +311,8 @@ export async function createAgentSession(options = {}) {
     }
     const session = new AgentSession({
         agent,
+        selectedModel,
+        usesModelRuntimeStream: true,
         sessionManager,
         settingsManager,
         cwd,
diff --git a/dist/core/session-manager.d.ts b/dist/core/session-manager.d.ts
index 76c53813b3c8de823afe3a34b4447da983450c26..7b6c4f89e1d55d99f72600061529f798aa52110d 100644
--- a/dist/core/session-manager.d.ts
+++ b/dist/core/session-manager.d.ts
@@ -1,5 +1,5 @@
 import type { AgentMessage } from "@earendil-works/pi-agent-core";
-import { type AssistantMessage, type ImageContent, type Message, type SystemMessage, type TextContent, type ToolResultMessage, type Usage, type UserMessage } from "@earendil-works/pi-ai";
+import { type AssistantMessage, type ImageContent, type Message, type Model, type SystemMessage, type TextContent, type ToolResultMessage, type Usage, type UserMessage } from "@earendil-works/pi-ai";
 import { type BashExecutionMessage, type CustomMessage } from "./messages.ts";
 export declare const CURRENT_SESSION_VERSION = 3;
 export interface SessionHeader {
@@ -54,6 +54,8 @@ export interface CompactionEntry<T = unknown> extends SessionEntryBase {
     usage?: Usage;
     /** True if generated by an extension, undefined/false if pi-generated (backward compatible) */
     fromHook?: boolean;
+    /** Why compaction ran. Missing on sessions created before reason tracking. */
+    reason?: "manual" | "threshold" | "overflow";
     /** Complete prompt and tool state at this compaction boundary. */
     systemMessage?: SystemMessage;
 }
@@ -151,6 +153,7 @@ export interface SessionProjection {
         provider: string;
         modelId: string;
     } | null;
+    reconstruction?: SessionContext["reconstruction"];
 }
 export interface SessionContext {
     messages: AgentMessage[];
@@ -159,7 +162,30 @@ export interface SessionContext {
         provider: string;
         modelId: string;
     } | null;
+    reconstruction?: {
+        schemaVersion: 1;
+        sourceCompactionId: string;
+        firstKeptEntryId: string | null;
+        droppedThroughEntryId: string | null;
+        targetIdentity: {
+            api: string;
+            provider: string;
+            baseUrl: string;
+            compactionBaseUrl: string;
+            modelId: string;
+            credentialFingerprint?: string;
+        };
+    };
 }
+export interface BuildSessionContextOptions {
+    forceNativeReconstruction?: boolean;
+    requestOverheadTokens?: number;
+}
+export declare function nativeCredentialFingerprint(apiKey?: string, headers?: Record<string, string>): string | undefined;
+export declare function withNativeCredentialFingerprint<T extends Model<any>>(model: T, apiKey?: string, headers?: Record<string, string>): T;
+export declare function getNativeCredentialFingerprint(model?: Model<any>): string | undefined;
+export declare function withPreparedProviderHeaders<T extends Model<any>>(model: T, headers: Record<string, string>): T;
+export declare function takePreparedProviderHeaders(model: Model<any>): Record<string, string> | undefined;
 export interface SessionInfo {
     path: string;
     id: string;
@@ -195,11 +221,13 @@ export declare function sessionEntryToContextMessages(entry: SessionEntry): Agen
  * by the kept entries starting at firstKeptEntryId and all entries after the
  * compaction entry. Older summarized entries are omitted.
  */
-export declare function buildContextEntries(entries: SessionEntry[], leafId?: string | null, byId?: Map<string, SessionEntry>): SessionEntry[];
+export declare function buildContextEntries(entries: SessionEntry[], leafId?: string | null, byId?: Map<string, SessionEntry>, targetModel?: Model<any>): SessionEntry[];
+/** Conservative model-facing token estimate used while fitting reconstructed history. */
+export declare function estimateReconstructionMessageTokens(message: AgentMessage): number;
 /** Build provenance-preserving, compaction-aware model context. */
-export declare function buildSessionProjection(entries: SessionEntry[], leafId?: string | null, byId?: Map<string, SessionEntry>): SessionProjection;
+export declare function buildSessionProjection(entries: SessionEntry[], leafId?: string | null, byId?: Map<string, SessionEntry>, targetModel?: Model<any>, options?: BuildSessionContextOptions): SessionProjection;
 /** Build the finalized model context from the canonical session projection. */
-export declare function buildSessionContext(entries: SessionEntry[], leafId?: string | null, byId?: Map<string, SessionEntry>): SessionContext;
+export declare function buildSessionContext(entries: SessionEntry[], leafId?: string | null, byId?: Map<string, SessionEntry>, targetModel?: Model<any>, options?: BuildSessionContextOptions): SessionContext;
 export declare function getDefaultSessionDir(cwd: string, agentDir?: string): string;
 /** Exported for testing */
 export declare function loadEntriesFromFile(filePath: string): FileEntry[];
@@ -261,7 +289,7 @@ export declare class SessionManager {
     /** Append model-attributed usage that does not participate in LLM context. Returns the appended entry. */
     appendUsage(kind: string, provider: string, model: string, usage: Usage, note?: string): UsageEntry;
     /** Append a compaction summary as child of current leaf, then advance leaf. Returns entry id. */
-    appendCompaction<T = unknown>(summary: string, firstKeptEntryId: string | null, tokensBefore: number, details?: T, fromHook?: boolean, usage?: Usage): string;
+    appendCompaction<T = unknown>(summary: string, firstKeptEntryId: string | null, tokensBefore: number, details?: T, fromHook?: boolean, usage?: Usage, reason?: "manual" | "threshold" | "overflow"): string;
     /** Append a custom entry (for extensions) as child of current leaf, then advance leaf. Returns entry id. */
     appendCustomEntry(customType: string, data?: unknown): string;
     /** Append a session info entry (e.g., display name). Returns entry id. */
@@ -306,13 +334,13 @@ export declare class SessionManager {
      * Build the active, compaction-aware entry list for context/rendering.
      * Uses tree traversal from current leaf.
      */
-    buildContextEntries(): SessionEntry[];
+    buildContextEntries(targetModel?: Model<any>): SessionEntry[];
     /**
      * Build the session context (what gets sent to the LLM).
      * Uses tree traversal from current leaf.
      */
-    buildSessionProjection(): SessionProjection;
-    buildSessionContext(): SessionContext;
+    buildSessionProjection(targetModel?: Model<any>, options?: BuildSessionContextOptions): SessionProjection;
+    buildSessionContext(targetModel?: Model<any>, options?: BuildSessionContextOptions): SessionContext;
     /**
      * Get session header.
      */
diff --git a/dist/core/session-manager.js b/dist/core/session-manager.js
index 0ddbb9cccaa7355ca44d4bbc768eaab8af57bac8..2145a54515741dca73722ecde19415984f99cc82 100644
--- a/dist/core/session-manager.js
+++ b/dist/core/session-manager.js
@@ -1,5 +1,5 @@
-import { getCurrentSystemMessage, uuidv7, } from "@earendil-works/pi-ai";
-import { randomUUID } from "crypto";
+import { getCurrentSystemMessage, uuidv7 } from "@earendil-works/pi-ai";
+import { createHash, randomUUID } from "crypto";
 import { appendFileSync, closeSync, createReadStream, existsSync, mkdirSync, openSync, readdirSync, readSync, statSync, writeFileSync, } from "fs";
 import { readdir, stat } from "fs/promises";
 import { basename, join, resolve } from "path";
@@ -9,6 +9,71 @@ import { APP_NAME, getAgentDir as getDefaultAgentDir, getSessionsDir } from "../
 import { normalizePath, resolvePath } from "../utils/paths.js";
 import { createBranchSummaryMessage, createCompactionSummaryMessage, createCustomMessage, } from "./messages.js";
 export const CURRENT_SESSION_VERSION = 3;
+const NATIVE_CREDENTIAL_FINGERPRINT = Symbol.for("pi.nativeCredentialFingerprint");
+const PREPARED_PROVIDER_HEADERS = new WeakMap();
+function codexAccountId(apiKey) {
+    try {
+        const parts = apiKey.split(".");
+        if (parts.length !== 3)
+            return undefined;
+        const payload = JSON.parse(Buffer.from(parts[1], "base64url").toString("utf8"));
+        const accountId = payload?.["https://api.openai.com/auth"]?.chatgpt_account_id;
+        return typeof accountId === "string" && accountId.length > 0 ? accountId : undefined;
+    }
+    catch {
+        return undefined;
+    }
+}
+function headerValue(headers, name) {
+    const expectedName = name.toLowerCase();
+    for (const [headerName, value] of Object.entries(headers ?? {})) {
+        if (headerName.toLowerCase() === expectedName && value.trim().length > 0)
+            return value.trim();
+    }
+    return undefined;
+}
+export function nativeCredentialFingerprint(apiKey, headers) {
+    const explicitAccountId = headerValue(headers, "chatgpt-account-id");
+    const authorization = headerValue(headers, "authorization");
+    const authorizationToken = authorization?.match(/^Bearer\s+(.+)$/i)?.[1];
+    const accountId = explicitAccountId ?? (authorizationToken ? codexAccountId(authorizationToken) : undefined) ??
+        (apiKey ? codexAccountId(apiKey) : undefined);
+    const headerEntries = Object.entries(headers ?? {}).sort(([left], [right]) => left.localeCompare(right));
+    const credentialMaterial = accountId
+        ? `codex-account:${accountId}`
+        : authorization
+            ? `authorization:${authorization}`
+            : apiKey
+                ? `api-key:${apiKey}`
+            : headerEntries.length > 0
+                ? `headers:${JSON.stringify(headerEntries)}`
+                : undefined;
+    return credentialMaterial
+        ? createHash("sha256").update(credentialMaterial).digest("hex")
+        : undefined;
+}
+export function withNativeCredentialFingerprint(model, apiKey, headers) {
+    const fingerprint = nativeCredentialFingerprint(apiKey, headers);
+    if (fingerprint) {
+        Object.defineProperty(model, NATIVE_CREDENTIAL_FINGERPRINT, { configurable: true, value: fingerprint });
+    }
+    else {
+        delete model[NATIVE_CREDENTIAL_FINGERPRINT];
+    }
+    return model;
+}
+export function getNativeCredentialFingerprint(model) {
+    return model?.[NATIVE_CREDENTIAL_FINGERPRINT];
+}
+export function withPreparedProviderHeaders(model, headers) {
+    PREPARED_PROVIDER_HEADERS.set(model, headers);
+    return model;
+}
+export function takePreparedProviderHeaders(model) {
+    const headers = PREPARED_PROVIDER_HEADERS.get(model);
+    PREPARED_PROVIDER_HEADERS.delete(model);
+    return headers;
+}
 function createSessionId() {
     return uuidv7();
 }
@@ -185,7 +250,7 @@ export function sessionEntryToContextMessages(entry) {
         return [createBranchSummaryMessage(entry.summary, entry.fromId, entry.timestamp)];
     }
     if (entry.type === "compaction") {
-        const summary = createCompactionSummaryMessage(entry.summary, entry.tokensBefore, entry.timestamp);
+        const summary = createCompactionSummaryMessage(entry.summary, entry.tokensBefore, entry.timestamp, entry.details, entry.reason);
         return entry.systemMessage ? [entry.systemMessage, summary] : [summary];
     }
     return [];
@@ -198,7 +263,27 @@ export function sessionEntryToContextMessages(entry) {
  * by the kept entries starting at firstKeptEntryId and all entries after the
  * compaction entry. Older summarized entries are omitted.
  */
-export function buildContextEntries(entries, leafId, byId) {
+function isCompatibleNativeCompaction(compaction, targetModel) {
+    const details = compaction.details;
+    if (details?.schemaVersion !== 1 || details?.mechanism !== "native")
+        return true;
+    if (!targetModel)
+        return true;
+    const supportsNativeCompaction = (targetModel.api === "openai-responses" ||
+        targetModel.api === "openai-codex-responses") &&
+        targetModel.compat?.supportsCompaction === true;
+    if (!supportsNativeCompaction)
+        return false;
+    const identity = details.identity;
+    return identity?.api === targetModel.api &&
+        identity?.provider === targetModel.provider &&
+        identity?.baseUrl === targetModel.baseUrl &&
+        identity?.compactionBaseUrl ===
+            (targetModel.compat?.compactionBaseUrl ?? targetModel.baseUrl) &&
+        identity?.modelId === targetModel.id &&
+        identity?.credentialFingerprint === getNativeCredentialFingerprint(targetModel);
+}
+export function buildContextEntries(entries, leafId, byId, targetModel) {
     const path = buildSessionPath(entries, leafId, byId);
     let compaction = null;
     for (const entry of path) {
@@ -209,6 +294,9 @@ export function buildContextEntries(entries, leafId, byId) {
     if (!compaction) {
         return path;
     }
+    if (!isCompatibleNativeCompaction(compaction, targetModel)) {
+        return path.filter((entry) => entry.type !== "compaction");
+    }
     const compactionIdx = path.findIndex((entry) => entry.id === compaction.id);
     if (compactionIdx < 0) {
         return path;
@@ -227,6 +315,110 @@ export function buildContextEntries(entries, leafId, byId) {
     contextEntries.push(...path.slice(compactionIdx + 1));
     return contextEntries;
 }
+export function estimateReconstructionMessageTokens(message) {
+    const textTokens = (text) => Math.max(text.length, Math.ceil(new TextEncoder().encode(text).length / 2));
+    const contentChars = (content) => {
+        if (typeof content === "string")
+            return textTokens(content);
+        if (!Array.isArray(content))
+            return 0;
+        return content.reduce((chars, block) => {
+            if (block.type === "text")
+                return chars + textTokens(block.text ?? "");
+            if (block.type === "image")
+                return chars + 4800;
+            return chars;
+        }, 0);
+    };
+    let chars = 0;
+    if (message.role === "user" || message.role === "toolResult" || message.role === "custom") {
+        chars = contentChars(message.content);
+    }
+    else if (message.role === "assistant") {
+        for (const block of message.content ?? []) {
+            if (block.type === "text")
+                chars += textTokens(block.text ?? "");
+            else if (block.type === "thinking")
+                chars += textTokens(block.thinking ?? "") + textTokens(block.thinkingSignature ?? "");
+            else if (block.type === "toolCall")
+                chars += textTokens(block.name ?? "") + textTokens(JSON.stringify(block.arguments ?? {}));
+        }
+    }
+    else if (message.role === "bashExecution") {
+        chars = textTokens(message.command ?? "") + textTokens(message.output ?? "");
+    }
+    else if (message.role === "branchSummary" || message.role === "compactionSummary") {
+        chars = textTokens(message.summary ?? "");
+    }
+    return chars;
+}
+function targetModelIdentity(model) {
+    return {
+        api: model.api,
+        provider: model.provider,
+        baseUrl: model.baseUrl,
+        compactionBaseUrl: model.compat?.compactionBaseUrl ?? model.baseUrl,
+        modelId: model.id,
+        credentialFingerprint: getNativeCredentialFingerprint(model),
+    };
+}
+function startsReconstructionUnit(messages, currentUnit) {
+    if (messages.some((message) => message.role === "bashExecution" ||
+        message.role === "branchSummary" ||
+        message.role === "compactionSummary"))
+        return true;
+    if (messages.some((message) => message.role === "user"))
+        return currentUnit.some((item) => item.messages.some((message) => message.role !== "custom"));
+    if (!messages.some((message) => message.role === "custom"))
+        return false;
+    return currentUnit.some((item) => item.messages.some((message) => message.role === "assistant" || message.role === "toolResult"));
+}
+function fitNativeReconstruction(entries, sourceCompaction, targetModel, requestOverheadTokens = 0) {
+    const units = [];
+    let currentUnit = [];
+    for (const projectedEntry of entries) {
+        const { sourceEntry: entry, messages } = projectedEntry;
+        const startsTurn = startsReconstructionUnit(messages, currentUnit);
+        if (startsTurn && currentUnit.some((item) => item.messages.length > 0)) {
+            units.push(currentUnit);
+            currentUnit = [];
+        }
+        currentUnit.push({ entry, messages, projectedEntry });
+    }
+    if (currentUnit.length > 0)
+        units.push(currentUnit);
+    const unitTokens = units.map((unit) => unit.reduce((total, item) => total + item.messages.reduce((subtotal, message) => subtotal + estimateReconstructionMessageTokens(message), 0), 0));
+    const contextWindow = Number.isFinite(targetModel.contextWindow) ? Math.max(0, targetModel.contextWindow) : Number.POSITIVE_INFINITY;
+    const outputReserve = Number.isFinite(targetModel.maxTokens) ? Math.max(0, targetModel.maxTokens) : 0;
+    const safetyReserve = Number.isFinite(contextWindow) ? Math.ceil(contextWindow * 0.25) : 0;
+    const requestReserve = Number.isFinite(requestOverheadTokens) ? Math.max(0, requestOverheadTokens) : 0;
+    const inputBudget = Number.isFinite(contextWindow)
+        ? Math.max(0, contextWindow - Math.max(outputReserve + requestReserve, safetyReserve))
+        : Number.POSITIVE_INFINITY;
+    let totalTokens = unitTokens.reduce((total, tokens) => total + tokens, 0);
+    let firstKeptUnit = 0;
+    while (totalTokens > inputBudget && firstKeptUnit < units.length) {
+        totalTokens -= unitTokens[firstKeptUnit];
+        firstKeptUnit++;
+    }
+    const keptItems = units.slice(firstKeptUnit).flat();
+    const messages = keptItems.flatMap((item) => item.messages);
+    const keptEntries = keptItems.map((item) => item.projectedEntry);
+    const droppedItems = units.slice(0, firstKeptUnit).flat();
+    const firstKeptEntry = keptItems.find((item) => item.messages.length > 0)?.entry;
+    const droppedThroughEntry = [...droppedItems].reverse().find((item) => item.messages.length > 0)?.entry;
+    return {
+        entries: keptEntries,
+        messages,
+        reconstruction: {
+            schemaVersion: 1,
+            sourceCompactionId: sourceCompaction.id,
+            firstKeptEntryId: firstKeptEntry?.id ?? null,
+            droppedThroughEntryId: droppedThroughEntry?.id ?? null,
+            targetIdentity: targetModelIdentity(targetModel),
+        },
+    };
+}
 /**
  * Build the session context from entries using tree traversal.
  * If leafId is provided, walks from that entry to root.
@@ -253,16 +445,20 @@ function projectContextEntry(entry, edit) {
     });
 }
 /** Build provenance-preserving, compaction-aware model context. */
-export function buildSessionProjection(entries, leafId, byId) {
+export function buildSessionProjection(entries, leafId, byId, targetModel, options = {}) {
     const path = buildSessionPath(entries, leafId, byId);
     const { thinkingLevel, model } = getSessionContextSettings(path);
-    const contextEntries = buildContextEntries(entries, leafId, byId);
+    const sourceCompaction = [...path].reverse().find((entry) => entry.type === "compaction");
+    const forceNativeReconstruction = options.forceNativeReconstruction === true;
+    const contextEntries = forceNativeReconstruction
+        ? path.filter((entry) => entry.type !== "compaction")
+        : buildContextEntries(entries, leafId, byId, targetModel);
     const edits = new Map();
     for (const entry of contextEntries) {
         if (entry.type === "context_edit")
             edits.set(entry.targetId, entry);
     }
-    const projectedEntries = contextEntries.map((sourceEntry, index) => ({
+    let projectedEntries = contextEntries.map((sourceEntry, index) => ({
         sourceEntry,
         // buildContextEntries() may retain an older compaction entry because its
         // raw ID lies inside the newest retained range. Only the newest compaction
@@ -271,6 +467,18 @@ export function buildSessionProjection(entries, leafId, byId) {
             ? []
             : projectContextEntry(sourceEntry, edits.get(sourceEntry.id)),
     }));
+    if (targetModel && sourceCompaction &&
+        (forceNativeReconstruction || !isCompatibleNativeCompaction(sourceCompaction, targetModel))) {
+        const fitted = fitNativeReconstruction(projectedEntries, sourceCompaction, targetModel, options.requestOverheadTokens);
+        projectedEntries = fitted.entries;
+        return {
+            entries: projectedEntries,
+            messages: fitted.messages,
+            thinkingLevel,
+            model,
+            reconstruction: fitted.reconstruction,
+        };
+    }
     return {
         entries: projectedEntries,
         messages: projectedEntries.flatMap((entry) => entry.messages),
@@ -279,8 +487,9 @@ export function buildSessionProjection(entries, leafId, byId) {
     };
 }
 /** Build the finalized model context from the canonical session projection. */
-export function buildSessionContext(entries, leafId, byId) {
-    const { messages, thinkingLevel, model } = buildSessionProjection(entries, leafId, byId);
+export function buildSessionContext(entries, leafId, byId, targetModel, options = {}) {
+    const { messages, thinkingLevel, model, reconstruction } = buildSessionProjection(entries, leafId, byId, targetModel, options);
+    return { messages, thinkingLevel, model, ...(reconstruction ? { reconstruction } : {}) };
     return { messages, thinkingLevel, model };
 }
 /**
@@ -877,7 +1086,7 @@ export class SessionManager {
         return entry;
     }
     /** Append a compaction summary as child of current leaf, then advance leaf. Returns entry id. */
-    appendCompaction(summary, firstKeptEntryId, tokensBefore, details, fromHook, usage) {
+    appendCompaction(summary, firstKeptEntryId, tokensBefore, details, fromHook, usage, reason) {
         const timestamp = new Date().toISOString();
         const systemMessage = getCurrentSystemMessage(this.buildSessionProjection().messages);
         const id = generateId(this.byId);
@@ -892,6 +1101,7 @@ export class SessionManager {
             details,
             usage,
             fromHook,
+            reason,
             ...(systemMessage ? { systemMessage: { ...systemMessage, timestamp: new Date(timestamp).getTime() } } : {}),
         };
         this._appendEntry(entry);
@@ -1074,19 +1284,37 @@ export class SessionManager {
      * Build the active, compaction-aware entry list for context/rendering.
      * Uses tree traversal from current leaf.
      */
-    buildContextEntries() {
-        return buildContextEntries(this.getEntries(), this.leafId, this.byId);
+    buildContextEntries(targetModel) {
+        return buildContextEntries(this.getEntries(), this.leafId, this.byId, targetModel);
     }
     /**
      * Build the session context (what gets sent to the LLM).
      * Uses tree traversal from current leaf.
      */
-    buildSessionProjection() {
-        return buildSessionProjection(this.getEntries(), this.leafId, this.byId);
-    }
-    buildSessionContext() {
-        const { messages, thinkingLevel, model } = this.buildSessionProjection();
-        return { messages, thinkingLevel, model };
+    buildSessionProjection(targetModel, options) {
+        const projection = buildSessionProjection(this.getEntries(), this.leafId, this.byId, targetModel, options);
+        const reconstruction = projection.reconstruction;
+        if (reconstruction) {
+            const alreadyRecorded = this.getBranch().some((entry) => entry.type === "custom" &&
+                entry.customType === "pi.compaction_reconstruction" &&
+                entry.data?.schemaVersion === reconstruction.schemaVersion &&
+                entry.data?.sourceCompactionId === reconstruction.sourceCompactionId &&
+                entry.data?.firstKeptEntryId === reconstruction.firstKeptEntryId &&
+                entry.data?.droppedThroughEntryId === reconstruction.droppedThroughEntryId &&
+                entry.data?.targetIdentity?.api === reconstruction.targetIdentity.api &&
+                entry.data?.targetIdentity?.provider === reconstruction.targetIdentity.provider &&
+                entry.data?.targetIdentity?.baseUrl === reconstruction.targetIdentity.baseUrl &&
+                entry.data?.targetIdentity?.compactionBaseUrl === reconstruction.targetIdentity.compactionBaseUrl &&
+                entry.data?.targetIdentity?.modelId === reconstruction.targetIdentity.modelId &&
+                entry.data?.targetIdentity?.credentialFingerprint === reconstruction.targetIdentity.credentialFingerprint);
+            if (!alreadyRecorded)
+                this.appendCustomEntry("pi.compaction_reconstruction", reconstruction);
+        }
+        return projection;
+    }
+    buildSessionContext(targetModel, options) {
+        const { messages, thinkingLevel, model, reconstruction } = this.buildSessionProjection(targetModel, options);
+        return { messages, thinkingLevel, model, ...(reconstruction ? { reconstruction } : {}) };
     }
     /**
      * Get session header.
diff --git a/dist/core/settings-manager.d.ts b/dist/core/settings-manager.d.ts
index 53d2865e9765524087976e9887b2e8e6b200ebd5..ac8696f159b6ef090697057a3022159f914a9a06 100644
--- a/dist/core/settings-manager.d.ts
+++ b/dist/core/settings-manager.d.ts
@@ -9,6 +9,7 @@ export interface CompactionSettings {
     enabled?: boolean;
     reserveTokens?: number;
     keepRecentTokens?: number;
+    thresholdPercent?: number;
     modelOverrides?: Record<string, CompactionModelOverride>;
 }
 export interface BranchSummarySettings {
@@ -231,11 +232,13 @@ export declare class SettingsManager {
     private getCompactionTokenSetting;
     getCompactionReserveTokens(model?: Pick<Model<string>, "provider" | "id">): number;
     getCompactionKeepRecentTokens(model?: Pick<Model<string>, "provider" | "id">): number;
+    getCompactionThresholdPercent(): number;
     /** Resolve each token setting through model override, ordinary setting, then built-in default. */
     getCompactionSettings(model?: Pick<Model<string>, "provider" | "id">): {
         enabled: boolean;
         reserveTokens: number;
         keepRecentTokens: number;
+        thresholdPercent: number;
     };
     getBranchSummarySettings(): {
         reserveTokens: number;
diff --git a/dist/core/settings-manager.js b/dist/core/settings-manager.js
index ed9d8d264da6f3315874bc909edeea0daef28b5a..f264f7dd1760b5012986badfa7340084a72fed66 100644
--- a/dist/core/settings-manager.js
+++ b/dist/core/settings-manager.js
@@ -586,12 +586,16 @@ export class SettingsManager {
     getCompactionKeepRecentTokens(model) {
         return this.getCompactionTokenSetting("keepRecentTokens", model);
     }
+    getCompactionThresholdPercent() {
+        return this.settings.compaction?.thresholdPercent ?? 80;
+    }
     /** Resolve each token setting through model override, ordinary setting, then built-in default. */
     getCompactionSettings(model) {
         return {
             enabled: this.getCompactionEnabled(),
             reserveTokens: this.getCompactionReserveTokens(model),
             keepRecentTokens: this.getCompactionKeepRecentTokens(model),
+            thresholdPercent: this.getCompactionThresholdPercent(),
         };
     }
     getBranchSummarySettings() {
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 52dcaafdf0fac5b42a733039103edb68a7538de3..25ac1b53f1b00b8e171798f16684f8338a828ffa 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -3,7 +3,7 @@ export { CONFIG_DIR_NAME, getAgentDir, getDocsPath, getExamplesPath, getPackageD
 export { AgentSession, type AgentSessionConfig, type AgentSessionEvent, type AgentSessionEventListener, type ModelCycleResult, type ParsedSkillBlock, type PromptOptions, parseSkillBlock, type SessionStats, } from "./core/agent-session.ts";
 export { readStoredCredential } from "./core/auth-storage.ts";
 export type { CacheWarmingDecision, CacheWarmingStatus } from "./core/cache-warmer.ts";
-export { type BranchPreparation, type BranchSummaryResult, type CollectEntriesResult, type CompactionResult, type CutPointResult, calculateContextTokens, collectEntriesForBranchSummary, compact, DEFAULT_COMPACTION_SETTINGS, estimateTokens, type FileOperations, findCutPoint, findTurnStartIndex, type GenerateBranchSummaryOptions, generateBranchSummary, generateSummary, generateSummaryWithUsage, getLastAssistantUsage, prepareBranchEntries, serializeConversation, shouldCompact, } from "./core/compaction/index.ts";
+export { type BranchPreparation, type BranchSummaryResult, type CollectEntriesResult, type CompactionResult, type CutPointResult, calculateContextTokens, collectEntriesForBranchSummary, compact, DEFAULT_COMPACTION_SETTINGS, estimatePortableCompactionRequestOverheadTokens, estimateTokens, type FileOperations, findCutPoint, findTurnStartIndex, type GenerateBranchSummaryOptions, generateBranchSummary, generateSummary, generateSummaryWithUsage, getLastAssistantUsage, prepareBranchEntries, prepareCompaction, selectCompactionMechanism, serializeConversation, shouldCompact } from "./core/compaction/index.ts";
 export { createEventBus, type EventBus, type EventBusController } from "./core/event-bus.ts";
 export type { AfterProviderResponseEvent, AgentActivityOutcome, AgentBeforeSettleEvent, AgentBeforeSettleEventResult, AgentEndEvent, AgentSettledEvent, AgentStartEvent, AgentToolResult, AgentToolUpdateCallback, AppKeybinding, AutocompleteProviderFactory, BashToolCallEvent, BeforeAgentStartEvent, BeforeAgentStartEventResult, BeforeProviderHeadersEvent, BeforeProviderRequestEvent, BeforeProviderRequestEventResult, BoundaryContextPreview, BoundaryResult, BoundaryState, BuildSystemPromptOptions, CacheWarmingDecisionEvent, CacheWarmingDecisionEventResult, CompactionEntryDraft, CompactOptions, ContextEditEntryDraft, ContextEvent, ContextEventResult, ContextUsage, ContextWithSystemEvent, CustomEntryDraft, CustomMessageEntryDraft, CustomToolCallEvent, EditToolCallEvent, EntryRenderer, EntryRenderOptions, ExecOptions, ExecResult, Extension, ExtensionActions, ExtensionAPI, ExtensionCommandContext, ExtensionCommandContextActions, ExtensionContext, ExtensionContextActions, ExtensionError, ExtensionEvent, ExtensionFactory, ExtensionFlag, ExtensionHandler, ExtensionRuntime, ExtensionShortcut, ExtensionUIContext, ExtensionUIDialogOptions, ExtensionWidgetOptions, FindToolCallEvent, GrepToolCallEvent, InlineExtension, InputEvent, InputEventResult, InputSource, KeybindingsManager, LoadExtensionsResult, LsToolCallEvent, MarkdownTransformContext, MarkdownTransformer, MessageEndEvent, MessageEndEventResult, MessageRenderer, MessageRenderOptions, MessageStartEvent, MessageUpdateEvent, ModelSelectEvent, ModelSelectSource, NormalizedBuildSystemPromptOptions, PowerShellToolCallEvent, ProjectTrustContext, ProjectTrustEvent, ProjectTrustEventDecision, ProjectTrustEventResult, ProjectTrustHandler, ProviderConfig, ProviderModelConfig, ReadToolCallEvent, RegisteredCommand, RegisteredTool, ResolvedCommand, ResourcesDiscoverEvent, ResourcesDiscoverResult, SessionBeforeCompactEvent, SessionBeforeCompactResult, SessionBeforeForkEvent, SessionBeforeForkResult, SessionBeforeSwitchEvent, SessionBeforeSwitchResult, SessionBeforeTreeEvent, SessionBeforeTreeResult, SessionBoundaryDraft, SessionCompactEvent, SessionCompactFailedEvent, SessionInfoChangedEvent, SessionShutdownEvent, SessionStartEvent, SessionTreeEvent, SlashCommandInfo, SlashCommandSource, SourceInfo, TerminalInputHandler, ThinkingLevelSelectEvent, ToolCallEvent, ToolCallEventResult, ToolDefinition, ToolExecutionEndEvent, ToolExecutionMode, ToolExecutionStartEvent, ToolExecutionUpdateEvent, ToolInfo, ToolRenderResultOptions, ToolResultEvent, ToolResultEventResult, TurnEndEvent, TurnEndEventResult, TurnStartEvent, UIPromptEndEvent, UIPromptKind, UIPromptStartEvent, UserBashEvent, UserBashEventResult, WidgetPlacement, WorkingIndicatorOptions, WriteToolCallEvent, } from "./core/extensions/index.ts";
 export { createExtensionRuntime, defineTool, discoverAndLoadExtensions, ExtensionRunner, isBashToolResult, isEditToolResult, isFindToolResult, isGrepToolResult, isLsToolResult, isPowerShellToolResult, isReadToolResult, isToolCallEventType, isWriteToolResult, wrapRegisteredTool, wrapRegisteredTools, } from "./core/extensions/index.ts";
@@ -17,8 +17,8 @@ export { DefaultPackageManager } from "./core/package-manager.ts";
 export type { ResourceCollision, ResourceDiagnostic, ResourceLoader } from "./core/resource-loader.ts";
 export { DefaultResourceLoader, loadProjectContextFiles } from "./core/resource-loader.ts";
 export { AgentSessionRuntime, type AgentSessionRuntimeDiagnostic, type AgentSessionServices, type CreateAgentSessionFromServicesOptions, type CreateAgentSessionOptions, type CreateAgentSessionResult, type CreateAgentSessionRuntimeFactory, type CreateAgentSessionRuntimeResult, type CreateAgentSessionServicesOptions, createAgentSession, createAgentSessionFromServices, createAgentSessionRuntime, createAgentSessionServices, createBashTool, createCodingTools, createEditTool, createFindTool, createGrepTool, createLsTool, createPowerShellTool, createReadOnlyTools, createReadTool, createWriteTool, type PromptTemplate, } from "./core/sdk.ts";
-export { type BranchSummaryEntry, buildContextEntries, buildSessionContext, buildSessionProjection, type CompactionEntry, type ContextEditableContent, type ContextEditEntry, CURRENT_SESSION_VERSION, type CustomEntry, type CustomMessageEntry, type FileEntry, getLatestCompactionEntry, type ModelChangeEntry, migrateSessionEntries, type NewSessionOptions, type ProjectedSessionEntry, parseSessionEntries, type SessionContext, type SessionEntry, type SessionEntryBase, type SessionHeader, type SessionInfo, type SessionInfoEntry, SessionManager, type SessionMessageEntry, type SessionProjection, type SessionTreeNode, sessionEntryToContextMessages, type ThinkingLevelChangeEntry, } from "./core/session-manager.ts";
-export { type CacheWarmingMode, type CompactionModelOverride, type CompactionSettings, type DefaultProjectTrust, type FullscreenExitOutput, type ImageSettings, type PackageSource, type RetrySettings, SettingsManager, type SettingsManagerCreateOptions, type TuiMode, } from "./core/settings-manager.ts";
+export { type BranchSummaryEntry, buildContextEntries, buildSessionContext, buildSessionProjection, type CompactionEntry, type ContextEditableContent, type ContextEditEntry, CURRENT_SESSION_VERSION, type CustomEntry, type CustomMessageEntry, estimateReconstructionMessageTokens, type FileEntry, getLatestCompactionEntry, type ModelChangeEntry, migrateSessionEntries, type NewSessionOptions, type ProjectedSessionEntry, parseSessionEntries, type SessionContext, type SessionEntry, type SessionEntryBase, type SessionHeader, type SessionInfo, type SessionInfoEntry, SessionManager, type SessionMessageEntry, type SessionProjection, type SessionTreeNode, sessionEntryToContextMessages, type ThinkingLevelChangeEntry } from "./core/session-manager.ts";
+export { type CacheWarmingMode, type CompactionModelOverride, type CompactionSettings, type DefaultProjectTrust, type FullscreenExitOutput, type ImageSettings, type PackageSource, type RetrySettings, SettingsManager, type SettingsManagerCreateOptions, type TuiMode } from "./core/settings-manager.ts";
 export { formatSkillsForPrompt, type LoadSkillsFromDirOptions, type LoadSkillsResult, loadSkills, loadSkillsFromDir, type Skill, type SkillFrontmatter, } from "./core/skills.ts";
 export { createSyntheticSourceInfo } from "./core/source-info.ts";
 export { type EditDiffResult, generateDiffString, generateUnifiedPatch } from "./core/tools/edit-diff.ts";
diff --git a/dist/index.js b/dist/index.js
index d8339661293e51358d0165680bad4334ff78f92e..44c3aba2462193b94d1ae787467c2159696ba02e 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -5,7 +5,7 @@ export { CONFIG_DIR_NAME, getAgentDir, getDocsPath, getExamplesPath, getPackageD
 export { AgentSession, parseSkillBlock, } from "./core/agent-session.js";
 export { readStoredCredential } from "./core/auth-storage.js";
 // Compaction
-export { calculateContextTokens, collectEntriesForBranchSummary, compact, DEFAULT_COMPACTION_SETTINGS, estimateTokens, findCutPoint, findTurnStartIndex, generateBranchSummary, generateSummary, generateSummaryWithUsage, getLastAssistantUsage, prepareBranchEntries, serializeConversation, shouldCompact, } from "./core/compaction/index.js";
+export { calculateContextTokens, collectEntriesForBranchSummary, compact, DEFAULT_COMPACTION_SETTINGS, estimatePortableCompactionRequestOverheadTokens, estimateTokens, findCutPoint, findTurnStartIndex, generateBranchSummary, generateSummary, generateSummaryWithUsage, getLastAssistantUsage, prepareBranchEntries, prepareCompaction, selectCompactionMechanism, serializeConversation, shouldCompact, } from "./core/compaction/index.js";
 export { createEventBus } from "./core/event-bus.js";
 export { createExtensionRuntime, defineTool, discoverAndLoadExtensions, ExtensionRunner, isBashToolResult, isEditToolResult, isFindToolResult, isGrepToolResult, isLsToolResult, isPowerShellToolResult, isReadToolResult, isToolCallEventType, isWriteToolResult, wrapRegisteredTool, wrapRegisteredTools, } from "./core/extensions/index.js";
 export { convertToLlm } from "./core/messages.js";
@@ -20,7 +20,7 @@ export { AgentSessionRuntime,
 createAgentSession, createAgentSessionFromServices, createAgentSessionRuntime, createAgentSessionServices, createBashTool,
 // Tool factories (for custom cwd)
 createCodingTools, createEditTool, createFindTool, createGrepTool, createLsTool, createPowerShellTool, createReadOnlyTools, createReadTool, createWriteTool, } from "./core/sdk.js";
-export { buildContextEntries, buildSessionContext, buildSessionProjection, CURRENT_SESSION_VERSION, getLatestCompactionEntry, migrateSessionEntries, parseSessionEntries, SessionManager, sessionEntryToContextMessages, } from "./core/session-manager.js";
+export { buildContextEntries, buildSessionContext, buildSessionProjection, CURRENT_SESSION_VERSION, estimateReconstructionMessageTokens, getLatestCompactionEntry, migrateSessionEntries, parseSessionEntries, SessionManager, sessionEntryToContextMessages } from "./core/session-manager.js";
 export { SettingsManager, } from "./core/settings-manager.js";
 // Skills
 export { formatSkillsForPrompt, loadSkills, loadSkillsFromDir, } from "./core/skills.js";
