diff --git a/dist/esm/activities/chat/index.js b/dist/esm/activities/chat/index.js
index 5eb60feb7b968cd9a803702aace1892cd3954837..34bb49ea27333e7acd46f7322209cbd944cbe635 100644
--- a/dist/esm/activities/chat/index.js
+++ b/dist/esm/activities/chat/index.js
@@ -305,6 +305,10 @@ class TextEngine {
     if (pendingToolCalls.length === 0) {
       return "continue";
     }
+    const argsMap = new Map();
+    for (const tc of pendingToolCalls) {
+      argsMap.set(tc.id, tc.function.arguments);
+    }
     const finishEvent = this.createSyntheticFinishedEvent();
     const { approvals, clientToolResults } = this.collectClientState();
     const generator = executeToolCalls(
@@ -350,7 +354,8 @@ class TextEngine {
       if (executionResult.results.length > 0) {
         for (const chunk of this.buildToolResultChunks(
           executionResult.results,
-          finishEvent
+          finishEvent,
+          argsMap
         )) {
           yield chunk;
         }
@@ -372,7 +377,8 @@ class TextEngine {
     }
     const toolResultChunks = this.buildToolResultChunks(
       executionResult.results,
-      finishEvent
+      finishEvent,
+      argsMap
     );
     for (const chunk of toolResultChunks) {
       yield chunk;
@@ -561,10 +567,30 @@ class TextEngine {
     }
     return chunks;
   }
-  buildToolResultChunks(results, finishEvent) {
+  buildToolResultChunks(results, finishEvent, argsMap) {
     const chunks = [];
     for (const result of results) {
       const content = JSON.stringify(result.result);
+      if (argsMap) {
+        chunks.push({
+          type: "TOOL_CALL_START",
+          timestamp: Date.now(),
+          model: finishEvent.model,
+          toolCallId: result.toolCallId,
+          toolName: result.toolName
+        });
+        const args = argsMap.get(result.toolCallId) ?? "{}";
+        if (args !== "{}") {
+          chunks.push({
+            type: "TOOL_CALL_ARGS",
+            timestamp: Date.now(),
+            model: finishEvent.model,
+            toolCallId: result.toolCallId,
+            delta: args,
+            args
+          });
+        }
+      }
       chunks.push({
         type: "TOOL_CALL_END",
         timestamp: Date.now(),
diff --git a/src/activities/chat/index.ts b/src/activities/chat/index.ts
index 74c5d5ce5734279d2ddfb7f3dacc76193534705e..2a79410912f01c43ba39dace2260e8ebec4620ad 100644
--- a/src/activities/chat/index.ts
+++ b/src/activities/chat/index.ts
@@ -641,6 +641,13 @@ class TextEngine<
       return 'continue'
     }
 
+    // Build args lookup so buildToolResultChunks can emit TOOL_CALL_START + TOOL_CALL_ARGS
+    // before TOOL_CALL_END during continuation re-executions.
+    const argsMap = new Map<string, string>()
+    for (const tc of pendingToolCalls) {
+      argsMap.set(tc.id, tc.function.arguments)
+    }
+
     const finishEvent = this.createSyntheticFinishedEvent()
 
     const { approvals, clientToolResults } = this.collectClientState()
@@ -699,6 +706,7 @@ class TextEngine<
         for (const chunk of this.buildToolResultChunks(
           executionResult.results,
           finishEvent,
+          argsMap,
         )) {
           yield chunk
         }
@@ -725,6 +733,7 @@ class TextEngine<
     const toolResultChunks = this.buildToolResultChunks(
       executionResult.results,
       finishEvent,
+      argsMap,
     )
 
     for (const chunk of toolResultChunks) {
@@ -997,12 +1006,39 @@ class TextEngine<
   private buildToolResultChunks(
     results: Array<ToolResult>,
     finishEvent: RunFinishedEvent,
+    argsMap?: Map<string, string>,
   ): Array<StreamChunk> {
     const chunks: Array<StreamChunk> = []
 
     for (const result of results) {
       const content = JSON.stringify(result.result)
 
+      // Emit TOOL_CALL_START + TOOL_CALL_ARGS before TOOL_CALL_END so that
+      // client-side StreamProcessor can reconstruct the full tool call.
+      // Without these, continuation re-executions only emit TOOL_CALL_END
+      // and the client stores empty arguments.
+      if (argsMap) {
+        chunks.push({
+          type: 'TOOL_CALL_START',
+          timestamp: Date.now(),
+          model: finishEvent.model,
+          toolCallId: result.toolCallId,
+          toolName: result.toolName,
+        })
+
+        const args = argsMap.get(result.toolCallId) ?? '{}'
+        if (args !== '{}') {
+          chunks.push({
+            type: 'TOOL_CALL_ARGS',
+            timestamp: Date.now(),
+            model: finishEvent.model,
+            toolCallId: result.toolCallId,
+            delta: args,
+            args,
+          })
+        }
+      }
+
       chunks.push({
         type: 'TOOL_CALL_END',
         timestamp: Date.now(),
