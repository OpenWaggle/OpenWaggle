---
title: "OpenRouter"
description: "Using OpenRouter to access 300+ models from multiple providers through a single API key."
order: 6
section: "Providers"
---

> **Coming soon** — OpenRouter support is implemented but not yet fully tested. It may not work reliably in the current release.

Disabled by default. OpenRouter is an aggregator that provides access to 300+ models from many providers through a single API key.

## Authentication

- **API key** — Get yours at [openrouter.ai/settings/keys](https://openrouter.ai/settings/keys)
- **OAuth** — Uses OpenRouter OAuth (returns a permanent API key). Connect via **Settings > Connections**.

