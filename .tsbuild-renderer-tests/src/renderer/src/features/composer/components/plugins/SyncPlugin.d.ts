/**
 * Syncs Lexical editor text content to the Zustand composer store.
 * This keeps `composer-store.input` current for submission, voice, history, and canSend checks.
 */
export declare function SyncPlugin(): null;
