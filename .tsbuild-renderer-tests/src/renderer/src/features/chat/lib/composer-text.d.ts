/**
 * Keeps the plain composer store and Lexical editor in sync when workflows switch
 * draft contexts outside the editor component itself.
 */
export declare function setComposerTextValue(text: string): void;
