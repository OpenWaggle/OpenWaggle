import { createLogger } from '../../../logger'

export const logger = createLogger('pi-agent-kernel')
export const WAGGLE_VISIBLE_USER_CUSTOM_TYPE = 'openwaggle.waggle.user_request'
export const WAGGLE_TURN_CUSTOM_TYPE = 'openwaggle.waggle.turn'
